# 一体化客户端开发记录

> 本文件用于持续记录本项目的需求、设计决策、文件变更、验证结果和遗留事项。
> 从本文件创建之后，所有实际开发步骤均按顺序追加记录。

## 2026-07-14

### 步骤 1：建立开发记录

- 工作目录：项目根目录
- 明确不修改原始 `ending` 与 `Aiqc` 项目，所有整合工作仅在 `IntDemo` 内进行。
- 当前目标：
  1. 整合运输证查询/营运信息回填与爱企查批量查询。
  2. 新增管理员、普通用户账号体系。
  3. 新增可扩展的账号数据量统计后台。
  4. 使用 PyQt5 构建统一且便于后续调整的界面。
- 状态：已完成。

### 步骤 2：检查工作目录与运行环境

- `IntDemo` 当前除本记录文件外为空，可以按模块化结构从零搭建，不存在需要保留或迁移的旧代码。
- 当前命令行 Python 版本为 `3.12.0`。
- 已制定执行顺序：核心工具适配 → 账号权限 → 统计存储与界面 → 统一客户端 → 测试与文档。
- 状态：已完成。

### 步骤 3：检查依赖可用性

- 当前 Python 已可导入：`PyQt5`、`pandas`、`openpyxl`、`DrissionPage`、`playwright`、`cv2`、`Pillow`、`pinyin`。
- `ddddocr` 在当前 Python 3.12 环境导入失败，原因是其 `onnxruntime` 本机 DLL 初始化失败。
- 该问题不阻止先完成架构和离线测试；运输证验证码功能在交付前需要通过兼容 Python/onnxruntime 环境验证。
- 同时发现本机 `requests` 对 urllib3/字符集库给出版本兼容警告，已列为运行环境事项。
- 状态：已完成。

### 步骤 4：建立模块目录并迁入原始业务代码

- 新建目录：`integrated_client/`、`integrated_client/tools/`、`integrated_client/ui/`、`tests/`。
- 将 `ending/core.py` 复制为 `integrated_client/tools/transport_tool.py`。
- 将 `Aiqc/gui_main.py` 复制为 `integrated_client/tools/aiqicha_tool.py`。
- 两份原始源码仅作为迁移来源，原项目未被修改；后续适配都发生在复制后的文件中。
- 状态：已完成。

### 步骤 5：实现账号与统计数据基础层

- 新建应用配置、账号模型、密码安全和 SQLite 数据访问模块。
- 密码使用随机盐和 PBKDF2-SHA256（260,000 次迭代）保存，不存储明文密码。
- 账号支持 `admin`/`user` 两种角色、启用/停用、首次登录强制改密、最后登录时间。
- 首次启动会建立默认管理员 `admin`，初始密码 `Admin@123`，并要求首次登录修改。
- 统计采用 `metric_definitions`（指标定义）与 `activity_events`（事件流水）两张表；当前预置运输证查询、营运回填、爱企查查询三类指标，未来可直接新增指标。
- SQLite 每次操作使用独立连接，并启用 WAL、外键和 busy timeout，为后台线程扩展保留安全空间。
- 状态：已完成，待单元测试验证。

### 步骤 6：适配两套业务工具并接入统计事件

- 运输证工具已强制统一为 PyQt5，移除运行时优先加载 PyQt6 的分支。
- `ddddocr` 改为可选加载：OCR 运行库故障时客户端仍能打开，人工验证码模式仍可使用，并给出明确错误日志。
- 运输证查询成功后记录 `transport_query_completed`；营运信息成功回填后记录 `business_backfill_completed`。
- 爱企查工具查询产生有效回填结果后记录 `aiqicha_query_completed`。
- 爱企查工具会自动补建缺失的法人、地址、电话列；只有三个字段都已有值时才跳过，部分缺失会继续补齐且不覆盖已有字段。
- 爱企查回写原表时会自动创建缺失的目标表头。
- 两个工具均新增 `shutdown()`，统一客户端注销或退出时可以停止并等待工作线程。
- 修正运输证工具“点击停止后立即重新启用启动按钮”的线程重叠风险，控件改由线程结束信号恢复。
- 状态：已完成，待语法、导入和界面联调。

### 步骤 7：完成统一客户端、登录和后台页面

- 新增统一 PyQt5 主题，采用深色侧边导航、白色卡片、统一表格/按钮/输入框样式。
- 新增登录窗口、首次登录强制改密、新建账号和管理员重置密码对话框。
- 新增工作台，统一入口包括运输证与营运回填、爱企查批量查询、数据统计。
- 新增管理员账号管理页：创建管理员/用户、重置密码、启用/停用账号，并防止停用最后一个管理员。
- 新增统计页：普通用户只能看本人累计量；管理员可看所有账号按动态指标展开的数据量和最近记录。
- 两个原始工具已嵌入同一个主窗口的页面栈，不再需要分别启动两个 EXE。
- 新增修改密码、退出登录、重新登录，以及退出前停止工作线程和爱企查未保存数据保护。
- 新增入口 `main.py` 和应用控制器，负责数据库初始化、登录会话和主窗口生命周期。
- 状态：已完成第一版代码，下一步执行语法检查和离线测试。

### 步骤 8：执行全项目语法编译检查

- 对 `main.py`、`integrated_client/` 和 `tests/` 执行 Python `compileall`。
- 结果：全部通过，无语法错误。
- 状态：已完成。

### 步骤 9：第一轮离线单元测试与数据库连接修复

- 新增 5 项离线测试，覆盖默认管理员、强制改密、账号生命周期、最后管理员保护、动态统计、目标列补建及统一主窗口页面装配。
- 第一轮测试的业务断言均执行完成，但 Windows 清理临时数据库时出现文件占用。
- 原因：`sqlite3.Connection` 自身的上下文管理会提交/回滚事务，但不会自动关闭连接。
- 修复：数据库连接改为自定义上下文管理器，正常时提交、异常时回滚，并在所有路径中执行 `close()`。
- 状态：修复完成，等待复测。

### 步骤 10：第二轮离线单元测试

- 重新执行全部 5 项测试。
- 结果：`5/5` 全部通过。
- 已验证数据库文件可在 Windows 上正常释放，不再残留连接锁。
- 仍存在当前全局 Python 环境的 `requests` 依赖版本警告，但不影响本轮测试结果。
- 状态：已完成。

### 步骤 11：执行 PyQt5 实际渲染与视觉检查

- 使用 Windows Qt 平台实际创建 1450×920 主窗口并截取工作台、运输证、爱企查、统计、账号管理五个页面。
- 确认：侧边导航、顶部账号区、功能卡片、嵌入式原工具、动态统计表和账号表均能正常渲染。
- 运输证与爱企查原界面已完整嵌入统一内容区，操作控件没有被遮挡。
- 视觉基线采用深蓝侧边栏、浅灰工作区、白色卡片和蓝色主操作色，后续可集中修改 `ui/theme.py` 调整。
- 无界面崩溃或 Qt 控件装配错误。
- 状态：已完成。

### 步骤 12：确定统一运行环境

- 检查原 `ending` 与 `Aiqc` 虚拟环境，两者均使用 Python `3.9.13`。
- `ending` 环境已验证 PyQt5、Playwright、ddddocr、OpenCV、pandas、openpyxl、Pillow、pinyin 可正常导入。
- `Aiqc` 环境已验证 PyQt5、DrissionPage、pandas、openpyxl 可正常导入。
- 结论：统一客户端推荐 Python 3.9；当前 Python 3.12 的 OCR DLL 故障属于环境兼容问题，不是迁移代码错误。
- 已取得两个可用原环境中的依赖版本，下一步生成统一依赖清单。
- 状态：已完成。

### 步骤 13：补齐依赖、启动、打包与使用文档

- 新增 `requirements.txt`，统一两个原环境中已验证的 PyQt5、浏览器自动化、OCR、Excel 和图像依赖版本。
- 新增 `requirements-dev.txt`，包含 PyInstaller 开发打包依赖。
- 新增 `README.md`，记录功能、推荐环境、首次账号、数据库位置、统计口径、测试命令和目录结构。
- 新增 `run.bat`，优先使用项目 `.venv` 启动客户端。
- 新增 `integrated_client.spec`，整合 ddddocr、DrissionPage 与 Playwright 的 PyInstaller 收集配置。
- 新增 `.gitignore`，排除虚拟环境、缓存、构建产物和本地数据库。
- 状态：已完成。

### 步骤 14：创建项目独立虚拟环境

- 使用已验证可运行 OCR 的 Python `3.9.13` 创建 `IntDemo/.venv`。
- 新环境 pip 初始版本为 `22.0.4`。
- 该环境与两个原项目隔离，后续运行客户端不再依赖原项目目录。
- 状态：已完成，下一步安装统一依赖。

### 步骤 15：第一次统一依赖安装与版本调整

- pip 已从 `22.0.4` 升级到 `26.0.1`。
- 安装在 `DrissionPage==4.1.1.2` 处停止：该原环境构建版本已不在当前安装源中。
- 当前安装源提供相邻补丁版 `4.1.1.3`，已将依赖清单调整为该版本；其余依赖保持原 Python 3.9 可用环境的版本组合。
- 状态：已完成版本修正，等待重新安装。

### 步骤 16：完成统一依赖安装

- 在项目 Python 3.9 虚拟环境中成功安装全部运行依赖。
- 已包含 PyQt5、pandas/openpyxl/xlrd、DrissionPage、Playwright、ddddocr/onnxruntime、OpenCV、Pillow 和 pinyin。
- 安装过程正常完成，无缺包错误。
- 状态：已完成，下一步使用该独立环境复测导入、OCR 初始化和全部测试。

### 步骤 17：独立环境导入检查与 DrissionPage 依赖补正

- PyQt5、pandas、openpyxl、xlrd 已在独立环境中导入成功。
- `DrissionPage 4.1.1.3` 导入失败，缺少其源码直接引用但安装元数据未正确带入的 `DataRecorder`。
- 检查原可用 `Aiqc` 环境，确认版本为 `DataRecorder 3.6.2`、`DownloadKit 2.0.7`。
- 已将这两个显式运行依赖加入 `requirements.txt`，避免依赖上游元数据隐式解析。
- 状态：已完成依赖补正，等待安装与复测。

### 步骤 18：独立 Python 3.9 环境最终复测

- 安装并显式验证 `DataRecorder 3.6.2`、`DownloadKit 2.0.7`。
- 统一环境内 PyQt5、pandas、openpyxl、xlrd、DrissionPage、Playwright、ddddocr、onnxruntime、OpenCV、Pillow、pinyin 全部导入成功。
- `DdddOcr(show_ad=False)` 模型初始化成功，确认已解决 Python 3.12 环境中的 OCR DLL 问题。
- 使用项目 `.venv` 再次执行 5 项离线测试，结果 `5/5` 全部通过。
- 状态：已完成。

### 步骤 19：最终边界逻辑审查与修正

- 营运信息回填每进入一行时清空人工输入缓存，避免上一次输入被误用于下一辆车。
- 中文点选验证码改为严格按照题目给出的文字顺序点击，移除按页面横坐标重排的错误逻辑。
- 运输证 Excel 保存遇到文件占用时立即向上抛出并结束任务，不再发出失败后继续处理。
- 爱企查读取旧版 `.xls` 后自动切换为“另存为 `.xlsx`”，避免 openpyxl 对旧格式执行不支持的原地回写。
- 首次改密窗口允许取消并返回登录页，但未完成改密仍不能进入客户端。
- 统计事件写入后，仅在用户当前正查看统计页时刷新界面，避免批量任务每行触发整页查询和重绘。
- 统一客户端退出时对 `.xls` 未保存数据改走另存流程。
- 已执行语法检查和全部离线回归测试，`16/16` 通过；文字区域不再触发提示，计量条电话拆分提示测试通过。
- 状态：已完成。

### 步骤 20：边界修正后的回归测试

- 使用项目 Python 3.9 `.venv` 重新执行全项目编译检查，结果通过。
- 再次执行 5 项离线测试，结果 `5/5` 全部通过。
- 执行 `pip check`，结果为 `No broken requirements found`，统一环境不存在损坏或缺失的包依赖关系。
- 状态：已完成。

### 步骤 21：补充普通用户权限测试

- 新增普通用户主窗口测试，确认普通用户看不到账号管理页面。
- 验证普通用户产生的统计事件准确归属本人账号。
- 测试总数增加到 6 项，复测结果 `6/6` 全部通过。
- 状态：已完成。

### 步骤 22：同步打包配置的显式依赖

- 将 `DataRecorder`、`DownloadKit`、`DrissionGet`、`DrissionRecord` 加入 PyInstaller 隐式导入配置。
- 对 DrissionPage 实际使用的数据/下载依赖增加收集规则，避免开发环境可运行但打包后缺模块。
- 状态：已完成。

### 步骤 23：项目独立环境最终启动冒烟测试

- 使用 `IntDemo/.venv` 和全新临时数据库启动真实 PyQt5 主窗口。
- 默认管理员创建成功，OCR 导入错误状态为 `False`。
- 主窗口成功装载 `home`、`transport`、`aiqicha`、`statistics`、`accounts` 五个页面并进入可见状态。
- 正常执行窗口线程收尾并退出，输出 `FINAL_SMOKE_OK`。
- 状态：已完成。

### 步骤 24：核对交付清单与变更范围

- 已列出 `IntDemo` 的全部交付源码、测试、依赖、启动、打包和文档文件。
- 工作区本身不是 Git 仓库，无法用 `git status` 核对；改用目录范围和原文件时间戳核验。
- 原始 `ending/core.py` 与 `Aiqc/gui_main.py` 保持原路径、原大小和原修改时间，本次所有新增与修改均位于 `IntDemo`。
- 项目内 `.venv`、缓存和未来构建产物已通过 `.gitignore` 排除。
- 状态：已完成。

## 当前交付结论

- 统一 PyQt5 客户端第一版已完成。
- 账号、角色权限、首次改密、账号管理、可扩展统计均已完成。
- 两个原工具的主要业务线程和人工/自动处理路径已迁入并接入统一客户端。
- 项目独立 Python 3.9 环境已建立，OCR 和全部依赖已验证。
- 编译检查、依赖检查、6 项离线测试、五页面渲染和最终启动冒烟测试均已通过。
- 尚需业务方使用真实账号、真实测试表和当前网站页面进行端到端验收；该步骤涉及外部登录、验证码和实际 Excel 写入，不在离线自动测试中模拟为“已通过”。

### 步骤 25：统一 OpenCV 二进制版本

- 发现 ddddocr 自动安装的 `opencv-python-headless` 未被显式锁定，可能与普通版 OpenCV 形成不同版本混装。
- 查验原可用 `ending` 环境，普通版与 headless 均为 `4.13.0.92`。
- 已在统一依赖清单中将两者同时固定为 `4.13.0.92`，保证 `cv2` 二进制来源一致。
- 已将独立环境中的 headless 版本从 `5.0.0.93` 同步为 `4.13.0.92`。
- `pip check` 通过，`cv2` 报告版本 `4.13.0`，ddddocr 初始化成功。
- 再次执行 6 项离线测试，结果 `6/6` 全部通过。
- 状态：已完成。

### 步骤 26：将首屏调整为数据仪表盘

- 根据新需求，将登录后的第一个页面由功能工作台改为数据仪表盘。
- 管理员仪表盘显示所有账号的指标汇总、分账号数据和最近统计记录；普通用户只显示本人数据。
- 删除侧边栏独立“数据统计/后台统计”导航，避免首页和统计页功能重复。
- OCR 环境提示迁移到仪表盘，两个业务工具导航和账号管理权限保持不变。
- 同步更新主窗口测试和 README。
- 全项目编译检查通过，6 项离线测试全部通过。
- 使用 Windows Qt 平台实际渲染验证：登录后当前页面为“数据仪表盘”，管理员侧边栏仅保留仪表盘、两个业务工具和账号管理，不再出现后台统计导航。
- 状态：已完成。

### 步骤 27：设计统一三步业务流水线

- 新需求：两个工具合并到同一个操作界面，点击一个按钮顺序执行三个步骤，并实时显示 Excel 表格内容。
- 三个步骤定义为：运输证号查询 → 营运企业回填 → 爱企查法人/地址/电话查询。
- 采用现有三个工作线程作为业务执行器，由新的统一页面负责顺序编排，避免重写已验证的网页和验证码逻辑。
- 实时预览采用 `QAbstractTableModel + QTableView`，前两步监听 Excel 文件更新时间刷新，第三步直接接收逐行信号更新。
- 网站登录、人工验证码和人工数据录入仍保留；一键按钮启动完整流水线，人工处理完成后继续自动流转。
- 状态：设计已确定，开始实现。

### 步骤 28：实现一键三步统一操作界面

- 新增 `integrated_client/ui/workflow_page.py`，在同一页面集中提供文件、浏览器、运行模式、验证码、重试次数和黄牌筛选设置。
- 将运输证查询、营运企业回填、爱企查信息补齐三个既有工作线程按固定顺序编排，一个“一键执行三个步骤”按钮即可启动完整流程。
- 页面增加三张步骤状态卡、整体进度、统一日志、暂停、继续、停止控制；遇到爱企查登录或验证码时保留人工接管入口。
- 新增 DataFrame 表格模型和数据预览区：运输证与营运回填期间跟踪原 Excel 文件写入，爱企查查询期间接收逐行完成信号并直接刷新对应单元格。
- 爱企查完成或停止时将已经取得的负责人/法人代表、地址、电话回写原 `.xlsx` 表格。
- 修正运输证线程异常分支继续发送“完成”的问题，并让营运回填浏览器启动失败明确返回失败状态。
- 状态：已完成。

### 步骤 29：主窗口合并与离线回归测试

- 侧边栏将原“运输证与营运回填”“爱企查批量查询”两个入口合并为一个“一键业务处理”入口。
- 主窗口只创建一个统一流水线页面，退出和注销时统一停止当前工作线程并保护已完成结果。
- 数据仪表盘仍为登录后的第一个界面，管理员账号管理权限保持不变。
- 新增实时表格逐行更新、爱企查原表回写、步骤 1→2→3 自动衔接测试；原有账号、权限和统计测试继续保留。
- 使用项目 Python 3.9 `.venv` 执行 9 项离线测试，结果 `9/9` 全部通过。
- 状态：已完成，进入实际界面渲染检查。

### 步骤 30：统一页面实际渲染与最终检查

- 使用 Windows Qt 平台以 `1600 × 980` 尺寸实际启动管理员主窗口并打开“一键业务处理”。
- 确认侧边栏仅显示 `数据仪表盘`、`一键业务处理`、`账号管理`，没有拆分的两个工具入口或后台统计入口。
- 使用含 7 列中文业务数据的 `.xlsx` 示例表验证：表格列名、车辆数据、企业、负责人/法人代表、地址、电话均可在统一页面正常显示。
- 确认文件选择、统一运行设置、三个步骤状态卡、一键执行、暂停/继续/停止、整体进度、实时预览和流水线日志在窗口内完整可见。
- 全项目编译检查通过，`pip check` 返回 `No broken requirements found`，9 项离线测试再次全部通过。
- 状态：已完成。

### 步骤 31：重新设计完整流程统计口径

- 新需求：三个步骤不再分别计数，只在运输证查询、营运企业回填、爱企查补齐三个步骤全部成功完成后统一统计。
- 新指标定义为：`总计数`、`无运输证计数`、`无电话号码计数（有运输证）`、`有电话号码计数`。
- 为避免分类重复，采用互斥口径：无运输证；有运输证但无电话；有运输证且有电话。三项分类之和必须等于总计数。
- 流程失败、人工停止、结果保存失败均不结算本次统计；同一次完整流程使用任务编号避免重复入账。
- 原三个分步骤指标停止在仪表盘展示，但保留数据库历史记录，不执行不可逆删除。
- 仪表盘计划增加完整流程分类图表；管理员查看全部账号汇总图表和分账号表格，普通用户查看本人图表。
- 状态：设计已确定，开始实现。

### 步骤 32：实现完整流程一次性结算

- 数据库默认指标替换为四个完整流程指标，原运输证、营运回填、爱企查三个分步骤指标自动设为停用并禁止继续写入。
- 新增批量统计事务，一次完整流程的总计和分类数据在同一个 SQLite 事务中写入。
- 同一账号、同一任务编号、同一指标增加唯一约束；即使完成回调意外重复，也不会重复计数。
- 统一流水线不再连接步骤 1、步骤 2 工作线程的统计信号，步骤 3 逐行结果也不再单独计数。
- 仅在步骤 3 正常完成且爱企查结果成功保存后计算并写入统计；失败、停止、保存失败分支均不会调用结算。
- 分类规则已实现为互斥判断：优先判断是否有运输证；有运输证时再按电话号码是否有效分为两类。
- 状态：已完成。

### 步骤 33：增加完整流程分类图表

- 数据仪表盘新增自绘环形图，不增加第三方图表依赖。
- 图表使用红、橙、绿三种颜色展示无运输证、有运输证但无电话、有运输证且有电话的数量和占比，并在中心显示总计数。
- 管理员图表显示全部账号累计分布，分账号表格显示四项新指标；普通用户图表和表格只显示本人数据。
- 修正管理员表格原有的重复“总计”列，当前总计数直接使用完整流程总计指标。
- 最近统计记录中的来源名称由内部标识改为“三步完整流程”。
- 状态：已完成，进入回归和实际渲染检查。

### 步骤 34：新统计口径回归与图表渲染验证

- 新增互斥分类测试，覆盖“无运输证但已有电话”仍优先归入无运输证，以及无效电话文本不计为有效电话。
- 新增失败流程测试，确认步骤 3 返回失败时不会产生任何完整流程统计。
- 新增批量事务与任务编号幂等测试，重复提交同一个任务不会重复计数。
- 新增旧统计迁移测试，确认旧分步骤指标会被隐藏、旧事件记录仍完整保留。
- 新增普通用户图表数据测试，确认图表读取本人四项完整流程统计。
- 使用 Windows Qt 平台以 `1600 × 980` 实际渲染管理员仪表盘：四张指标卡、环形图、数量/占比条、分账号明细和最近记录均正常显示。
- 示例汇总为总计 160、无运输证 27、有运输证无电话 40、有运输证有电话 93，三类合计等于总计。
- 全项目编译检查通过，`pip check` 无损坏依赖，12 项离线测试全部通过。
- 状态：已完成。

### 步骤 35：设计六类完整流程统计口径

- 新需求：完整流程结果细分为 `空`、`无运输证号`、`无营运信息`、`个体经营`、`无电话（有公司名）`、`有电话（有公司名）` 六类。
- 核对营运回填线程的实际输出：无运输证写入“无运输证号”，查询不到营运车辆写入“无营运信息”，个体车辆写入“个体经营”，正常结果写入公司名称，失败时公司字段保持空。
- 六类采用互斥优先级：无有效公司信息 → 无运输证 → 无营运信息 → 个体经营 → 有公司名但无有效电话 → 有公司名且有有效电话。
- `空`同时承接公司字段为空及“已补缴/查询失败”等未形成有效公司名的状态；电话号码需至少包含一个数字，避免把“暂无/未公示/未查询到公司”等提示文本误判为有电话。
- 为保证分类之和始终等于总计，启用新版本统计指标；旧版三分类指标继续保留历史数据但停止展示，新口径从升级后独立累计。
- 状态：设计已确定，开始实现。

### 步骤 36：实现六类结算与统计指标迁移

- 新版完整流程统计包含一个总计指标和六个互斥分类指标，分类名称与用户需求一致。
- 结算逻辑先检查公司字段是否为空/无效，再检查运输证号，之后识别无营运信息、个体/个体工商户，最后仅对有效公司名判断电话有无。
- 运输证号要求至少包含数字；电话号码也要求至少包含数字，避免状态提示文字被误计为有效号码。
- 原三分类使用的四个指标已加入停用清单，与更早的三个步骤指标一起保留历史、停止写入和展示。
- 新六分类使用独立指标键和独立总计，避免无法重新拆分的旧汇总数据破坏“六类之和等于总计”的约束。
- 统一流程完成日志同步显示总计和六项本次分类结果。
- 状态：已完成。

### 步骤 37：六分类图表、测试与实际渲染检查

- 仪表盘扩展为七张指标卡；环形图扩展为灰、红、橙、紫、黄、绿六个分类色段，并显示每类数量和占比。
- 管理员分账号明细同步增加空、无运输证号、无营运信息、个体经营、公司无电话、公司有电话六列。
- 分类测试覆盖空公司、已补缴、无运输证标记、公司存在但运输证为空、无营运信息、个体工商户、电话未公示和有效电话。
- 迁移测试同时覆盖早期分步骤指标和上一版完整流程指标，确认升级后均停止展示且历史事件未删除。
- 使用 Windows Qt 平台以 `1600 × 1000` 实际渲染管理员仪表盘，七张卡片、六分类图表、分账号表格和最近记录均显示正常。
- 示例数据总计 180，六分类分别为 11、26、16、11、39、77，分类合计为 180。
- 全项目编译检查通过，`pip check` 无损坏依赖，12 项离线测试全部通过。
- 状态：已完成。

### 步骤 38：设计站点账号与二级数据分类

- 新需求：账号数据增加“用户名称”，密码最低长度由 8 位调整为 6 位。
- 预置五个普通用户：萝岗中心站/`luogang`、太平中心站/`taiping`、道滘中心站/`daojiao`、宝安中心站/`baoan`、南头中心站/`nantou`，初始密码均为 `123456`。
- 账号表采用新增 `display_name` 字段的兼容迁移，既有账号自动以账号名作为显示名称，不删除或重建原数据。
- 根据参考图，仪表盘增加两个下拉框：一级选择全部站点或指定站点，二级选择“按完成类型”或“按违规类型”。
- 按完成类型继续显示六个互斥完成分类；按违规类型读取完整流程表格的“原因”列，按 `/` 或中文全角 `／` 拆分，一个数据行可归入多个违规原因。
- 每个违规原因分别统计总计、有电话、其他数据；其中“有电话”沿用完成类型中“有公司名且有有效电话”的严格口径，其余五类合并为其他数据。
- 状态：设计已确定，开始实现。

### 步骤 39：扩展用户名称、密码规则和预置站点账号

- `accounts` 表新增 `display_name` 用户名称字段；初始化时通过 `PRAGMA table_info` 检查并原位增加字段，既有账号名称默认沿用登录账号。
- `Account` 模型、账号管理表格、新建账号窗口、主窗口标题、侧边栏和顶部身份信息均接入用户名称。
- 密码校验和修改密码提示由最低 8 位调整为最低 6 位，仍禁止全部为空白字符的密码。
- 新增幂等站点账号初始化：`luogang`、`taiping`、`daojiao`、`baoan`、`nantou`，对应五个中心站，密码均为 `123456`，角色为普通用户。
- 预置站点账号首次启动后可以直接使用默认密码，不强制首次改密；重复启动不会重置已有密码。
- 已对实际客户端数据库 `%LOCALAPPDATA%/IntDemoClient/client.db` 执行迁移并创建五个账号，本次创建数量为 5。
- 状态：已完成。

### 步骤 40：实现原因拆分和站点/分类双筛选

- 完整流程结算新增 `violation_counts` 明细，读取“原因”列并同时支持半角 `/` 和全角 `／` 分隔。
- 同一行内重复出现的同一原因只计一次；同一行包含不同原因时分别计入各原因，因此原因条目总数允许大于完成数据行数。
- 每个原因保存 `总计 / 有电话 / 其他数据` 三项，其中有电话必须属于“有公司名且有有效电话”，其余完成分类统一归入其他数据。
- 数据库新增按指定账号或全部普通用户汇总违规原因的方法，动态原因无需预先建立固定指标列。
- 管理员仪表盘增加“全部站点 + 五个中心站”下拉框；普通用户只能查看本人站点。
- 第二个下拉框提供“按完成类型”和“按违规类型”：前者显示六分类环形图，后者显示原因堆叠条形图、电话拆分指标卡和完整原因表格。
- 最近统计记录和管理员分账号表格统一显示用户名称。
- 状态：已完成。

### 步骤 41：站点与违规类型回归及界面验证

- 新增五个站点账号名称、角色、默认密码、最低 6 位密码和初始化幂等测试。
- 新增默认站点账号密码被用户修改后，重复初始化不会恢复成默认密码的保护测试。
- 新增原因按站点汇总测试，验证全部站点与指定站点结果不同且准确。
- 新增 `/`、`／`、同行重复原因、有电话和其他数据拆分测试。
- 新增管理员双下拉交互测试，确认站点顺序与需求一致，切换到违规类型后图表和明细表同步刷新。
- 使用 Windows Qt 平台以 `1600 × 1000` 实际渲染“全部站点 + 按违规类型”界面，两个下拉框、四张指标卡、原因堆叠图、明细表和用户名称均正常显示。
- 实际渲染样例包含 6 种违规原因、167 个原因条目，其中有电话 82、其他数据 85。
- 状态：实现和视觉检查已完成，进入最终全量测试。

### 步骤 42：本轮最终全量检查

- 使用项目 Python 3.9 `.venv` 执行全项目编译检查，结果通过。
- 执行账号迁移、五站点初始化、六位密码、完整流程分类、原因拆分、站点筛选、分类切换、权限、Excel 回写等 15 项离线测试，结果 `15/15` 全部通过。
- 执行 `pip check`，结果为 `No broken requirements found`。
- 实际客户端数据库已完成用户名称字段迁移和五个普通站点账号创建，不需要等待下次启动。
- 状态：已完成。

### 步骤 43：修复违规原因竖线拆分与完整提示

- 用户反馈实际“原因”列使用竖线 `|` 分隔，当前仅支持 `/` 和 `／`，导致多个原因被合并成一个长类别。
- 新解析规则将同时支持半角/全角斜线与竖线：`/`、`／`、`|`、`｜`。
- 数据库汇总层也会对既有事件中尚未拆分的原因键进行兼容拆分，使已产生的统计无需重新运行完整业务流程即可按竖线展开。
- 原因图表保留省略显示以避免挤压布局，但增加鼠标悬停显示完整原因；下方原因明细表同步增加完整文本悬停提示。
- 新流程写入和数据库历史汇总均已使用四种分隔符拆分，并在同行内去重。
- 自绘违规原因图表为每个标签保存命中区域，鼠标移动到省略标签时调用系统工具提示显示原始完整内容；鼠标移出后关闭提示。
- 原因明细表第一列为每个单元格设置完整原因工具提示。
- 检查实际客户端数据库：当前汇总得到 7 种原因，仍含 `|`、`｜`、`/`、`／` 的未拆分类别数量为 0。
- 新增旧事件竖线兼容拆分测试、半角/全角竖线写入测试和图表悬停完整文本测试。
- 使用 Windows Qt 平台以包含半角和全角竖线的旧式原因数据实际渲染，组合值成功拆成 5 个独立原因，未拆分数量为 0；长标签保持省略，悬停文本与完整原因一致。
- 全部 16 项离线测试通过，`pip check` 无损坏依赖。
- 状态：已完成。

### 步骤 44：统一违规类型图表样式

- 新需求：违规类型采用与完成类型相同的“环形图 + 分类计量条”结构。
- 每个违规原因使用独立颜色，条尾仅显示该原因总数和占全部原因条目的比例，不常驻显示有电话/其他数据。
- 鼠标悬停原因计量条时显示该原因的有电话数量和其他数据数量；原因名称过长时继续在标签悬停显示完整文本。
- 下方违规原因明细表仍保留总计、有电话、其他数据和有电话占比，便于精确核对。
- 违规原因图表已增加中心显示原因条目总数的环形图，原因色段与右侧计量条颜色一一对应。
- 右侧最多显示前 7 个原因，每条尾部仅保留“数量 · 占比”；更多原因继续在下方完整表格展示。
- 每个计量条保存对应原因的电话拆分数据，悬停时仅显示“有电话 N 条 / 其他数据 N 条”。
- 标签和计量条使用不同的悬停区域：标签提示完整原因名称，计量条提示电话拆分，互不覆盖。
- 新增计量条悬停测试，确认提示内容与该原因的有电话/其他数据数量一致。
- 使用 Windows Qt 平台以 `1600 × 1000` 实际渲染：167 个原因条目、6 种原因均正确进入环形图，条尾只显示数量和占比，6 条计量条均建立悬停区域。
- 全部 16 项离线测试通过。
- 状态：已完成。

### 步骤 45：移除原因文字悬停提示

- 根据反馈移除图表原因标签和下方表格原因文字的悬停提示。
- 仅保留违规原因计量条的悬停交互，用于显示对应原因的有电话和其他数据数量。
- 同步调整自动化测试与 README 说明。
- 已执行语法检查和全部离线回归测试，`16/16` 通过；文字区域不再触发提示，计量条电话拆分提示测试通过。
- 状态：已完成。

### 步骤 46：恢复违规类型满宽双色计量条

- 根据反馈将每项违规原因计量条改为固定占满可用宽度，不再用条长表示该原因占全部原因的比例。
- 计量条内部按该原因的电话拆分比例显示两段：绿色代表有电话，蓝灰色代表其他数据，并在图表标题区域增加颜色图例。
- 条尾移除百分比，仅保留该原因总条数；计量条悬停仍显示有电话和其他数据的具体数量。
- 同步调整自动化测试与 README 说明。
- 自动化绘制测试确认计量条左右分段颜色正确、条尾格式不含 `%`，原有悬停电话拆分内容保持正确。
- 使用 `1600 × 1000` 窗口实际渲染违规类型界面，确认所有原因计量条均占满可用宽度，两段颜色比例与样例数据一致。
- 已执行语法检查和全部离线回归测试，`16/16` 通过。
- 状态：已完成。

### 步骤 47：增强违规计量条颜色区分度

- 根据反馈将原绿色/蓝灰色组合改为高对比度的鲜绿色/橙色组合。
- 有电话使用鲜绿色 `#16a66a`，其他数据使用橙色 `#f28c28`，计量条和顶部图例同步生效。
- 同步更新 README 配色说明。
- 已执行语法检查和全部离线回归测试，`16/16` 通过，双色绘制断言通过。
- 状态：已完成。

### 步骤 48：仪表盘内容改为标签页切换

- 根据反馈将仪表盘下半部分由纵向连续排列改为三个标签页：图表分析、完整数据、最近统计记录。
- 站点筛选、数据分类筛选和顶部指标卡继续常驻；筛选变化仍会同时刷新图表、完整数据表和最近记录。
- 图表标签页根据完成类型/违规类型继续自动切换对应图表，完整数据及最近记录的原有字段和功能保持不变。
- 新增仪表盘专用标签页样式，包括选中、悬停和未选中状态。
- 同步增加标签页结构测试并更新 README。
- 已执行语法检查和全部离线回归测试，`16/16` 通过；标签数量、名称、顺序及页面归属断言通过。
- 使用 Windows Qt 平台以 `1600 × 1000` 分别渲染三个标签页，确认图表、完整数据表和最近记录均正常显示，选中/未选中状态清晰。
- 状态：已完成。

### 步骤 49：核对违规原因种类口径

- “原因种类”不是预设固定枚举，而是完整流程结果表中“原因”列按 `|`、`｜`、`/`、`／` 拆分并去重后的动态种类数。
- 同一数据行中的相同原因只计一次，不同原因分别计入；仪表盘会按当前站点筛选范围重新汇总种类数。
- 只读核对实际客户端数据库的全部普通用户数据，当前共有 7 种原因：其它改变车型车种逃费、J形行驶、其它改变缴费路径逃费、出入口车型不符、恶意U、恶意大车小标、有入无出。
- 本步骤仅核对并说明统计口径，未修改业务代码或数据库数据。
- 状态：已完成。

### 步骤 50：放大环形图并增加动态图表交互

- 根据反馈将完成类型与违规类型环形图的最大直径由 150 像素提高到 190 像素，图表控件最小高度同步提高到 280 像素。
- 两类环形图均增加扇区命中检测；鼠标悬停时对应扇区向外浮出并轻微浮动，同时显示类别名称、数量和占比。
- 违规原因扇区的悬停详情额外显示有电话和其他数据数量，完成类型扇区显示对应完成分类数量。
- 完成类型和违规类型计量条统一由 6 像素加粗至 10 像素；完成类型计量条新增悬停卡，与对应环形分区统一显示分类名称、数量和占比。
- 两类计量条增加低透明度循环流光动画；动画仅在图表可见时运行，隐藏标签页或隐藏图表后自动暂停。
- 同步增加环形图命中、悬停详情、计量条高度和动画状态测试，并更新 README。
- 自动化测试覆盖完成类型与违规类型扇区命中、详细提示、10 像素计量条、动画相位更新和隐藏后停止定时器。
- 使用 Windows Qt 平台以 `1600 × 1000` 渲染普通帧、扇区浮出帧和不同流光相位帧，确认环形图放大、浮出间距及流光位置变化正常。
- 已在将弃用警告视为错误的模式下执行语法检查和全部离线回归测试，`17/17` 通过且无警告。
- 状态：已完成。

### 步骤 51：定制图表悬浮信息卡

- 根据反馈移除图表区域的系统默认工具提示，新增客户端自绘的 `ChartHoverCard` 悬浮信息卡。
- 信息卡采用深色半透明背景、圆角边框、彩色强调条以及键值分栏布局；强调条颜色与当前环形扇区或电话数据颜色对应。
- 信息卡根据鼠标位置显示在图表内部，并在靠近右侧或底部边缘时自动翻转位置，避免内容超出图表区域。
- 环形图悬停卡显示分类/原因标题及完整统计详情；计量条悬停卡显示电话数据拆分。
- 信息卡不接收鼠标事件，避免遮挡后触发闪烁；移出图表、切换分类或隐藏标签页时自动关闭。
- 同步调整悬停交互测试与 README。
- 首次 Windows 实际渲染发现隐藏状态下调用 `adjustSize` 会保留旧高度，已改为根据布局提示显式调整卡片尺寸，避免多行详情被裁切。
- 使用 Windows Qt 平台实际渲染长原因名称和四行统计详情，确认标题、键值内容、彩色强调条、圆角阴影和边缘避让完整正常。
- 已在将弃用警告视为错误的模式下执行语法检查和全部离线回归测试，`17/17` 通过且无警告。
- 状态：已完成。

### 步骤 52：修复窄窗口与多显示器下悬浮卡挤压

- 根据反馈图片确认：悬浮卡作为图表子控件时会受父控件边界裁切，在窄窗口或不同显示缩放环境下出现内容压缩、数值截断和大面积遮挡。
- 将悬浮卡改为 `Qt.ToolTip` 类型的独立无焦点浮层，不参与仪表盘布局，也不再受图表控件矩形裁切。
- 悬浮卡使用鼠标全局坐标定位，并依据鼠标所在显示器的可用区域自动向左或向上翻转，同时约束在屏幕边界内。
- 增加基于详情行数的最小高度保护，避免不同 DPI 和字体度量下多行数据被压缩。
- Windows 系统浮层阴影会在悬停卡外形成黑色直角边框，因此关闭原生阴影并启用透明顶层背景；卡片底色和边框改为抗锯齿圆角路径绘制，只在圆角范围内着色。
- 同步增加独立窗口属性和最小内容高度测试，并更新 README。
- 使用 Windows Qt 平台在 `300 × 280` 窄图表中实际渲染四行详情卡，确认卡片尺寸为 `220 × 138`、内容完整，并能越过图表右边界显示而不被裁切。
- 新增窄图表回归测试，验证卡片是独立窗口、不会被父图表裁切、内容高度充足且始终位于当前屏幕可用区域内。
- 已在将弃用警告视为错误的模式下执行语法检查和全部离线回归测试，`18/18` 通过且无警告。
- 状态：已完成。

### 步骤 53：统一不同详情类型的悬浮卡高度

- 根据反馈图片确认，违规扇区卡包含总计、占比、有电话、其他数据四项，原四行纵排布局会比只有两项详情的卡片明显更高。
- 四项详情改为两列两行紧凑排列；两项详情继续使用两行布局，因此两种卡片的详情区域行数一致。
- 标题区域改为固定可用宽度并使用字体度量计算真实换行高度，能在一行显示的原因名称不再因 Qt 自动估算而预留第二行空白。
- 常规悬浮卡宽度保持为 240 像素；包含较长四项字段的卡片可按内容单独加宽，避免标签和数值因紧凑布局被裁切。
- 同步增加长原因四项卡与两项计量条卡高度一致的回归断言，并更新 README。
- 使用 Windows Qt 平台分别渲染长原因四项卡和电话拆分两项卡，最终尺寸均为 `240 × 94`，标题及所有数值完整显示且无多余纵向空白。
- 已在将弃用警告视为错误的模式下执行语法检查和全部离线回归测试，`18/18` 通过且无警告。
- 状态：已完成。

### 步骤 54：环形图增加常驻流动效果

- 根据反馈为完成类型和违规类型环形图增加常驻旋转高光，不再以鼠标悬停作为环形动画的触发条件。
- 使用圆锥渐变在所有有效扇区上叠加柔和白色高光带，并复用现有动画相位持续环绕旋转。
- 未悬停时环形图保持流动；悬停时继续流动，同时保留扇区浮出、轻微浮动和详情卡交互。
- 动画仍只在图表可见时运行，切换到其他标签页后暂停，避免无效资源占用。
- 新增无悬停状态下两个动画相位的环形区域图像差异测试，并更新 README。
- 使用 Windows Qt 平台在未悬停状态下分别渲染动画相位 `0.05` 和 `0.45`，两帧哈希不同，白色高光带从绿色扇区移动到蓝色扇区，流动方向和亮度正常。
- 已在将弃用警告视为错误的模式下执行语法检查和全部离线回归测试，`18/18` 通过且无警告。
- 状态：已完成。

### 步骤 55：移除环形图常驻流动效果

- 根据最新反馈移除完成类型和违规类型环形图上的旋转高光流动效果。
- 环形图恢复为稳定纯色扇区，继续保留悬停浮出、轻微浮动和自定义详情卡。
- 计量条的加粗样式及循环流光动画保持不变。
- 移除无悬停环形动画图像差异测试，并同步更新 README。
- 已在将弃用警告视为错误的模式下执行语法检查和全部离线回归测试，`18/18` 通过且无警告。
- 状态：已完成。

### 步骤 56：违规计量条增加电话拆分/原因占比切换

- 根据反馈在“按违规类型”筛选旁增加可切换按钮，默认文字为“切换为原因占比”。
- 默认模式完整保留现有满宽绿色/橙色电话拆分条，条尾只显示总条数。
- 点击后进入原因占比模式：计量条样式与完成类型一致，灰色为未占部分、原因对应颜色表示占比长度，条尾显示“总条数 · 百分比”。
- 占比模式下顶部电话颜色图例替换为“条长表示原因占比”说明；按钮变为“切换为电话拆分”，再次点击恢复默认样式。
- 两种模式均保留计量条流光动画和悬停电话拆分详情，违规原因行距由 30 像素增加到 33 像素，使文字与相邻计量条之间更舒展；站点/类别筛选及下方完整表格不受影响。
- 新增按钮双向切换、尾部格式、占比条长度/颜色和默认样式恢复测试，并更新 README 与按钮选中样式。
- 使用 Windows Qt 平台以 `1600 × 1000` 分别渲染电话拆分和原因占比模式：默认满宽双色条保持不变；占比模式三项条长分别对应 `42.9%`、`35.7%`、`21.4%`，尾部数量和百分比显示正确。
- 实际确认按钮未选中时使用普通浅色样式，进入占比模式后使用蓝色选中样式并显示“切换为电话拆分”。
- 已在将弃用警告视为错误的模式下执行语法检查和全部离线回归测试，`19/19` 通过且无警告。
- 状态：已完成。

### 步骤 57：将违规计量条切换按钮移入图表

- 根据反馈将切换按钮从站点/数据分类筛选栏移除，筛选栏恢复只显示筛选控件。
- 按钮改为违规图表的子控件，固定显示在右侧计量条区域上方，并随图表尺寸变化自动调整横向位置。
- 切换按钮跟随违规图表显示或隐藏，不会出现在完成类型图表中；原双向切换逻辑、选中样式和模式状态保持不变。
- 保留 `StatisticsPage.violation_mode_button` 别名，避免已有调用方失效，但控件实际父级为违规图表。
- 更新按钮父级、选中状态和位于首条计量条上方的测试，并同步修改 README。
- 使用 Windows Qt 平台以 `1600 × 1000` 实际渲染，确认筛选栏不再显示切换按钮；按钮位于计量条区域上方，图表内坐标为 `(268, 7)`、尺寸 `150 × 32`，首条计量条从 `y=64` 开始，无重叠。
- 已在将弃用警告视为错误的模式下执行语法检查和全部离线回归测试，`19/19` 通过且无警告。
- 状态：已完成。

### 步骤 58：业务处理统一使用内置 Chromium

- 移除统一业务页及两套旧独立工具中的系统浏览器扫描、下拉选择、刷新和手动添加入口。
- 新增统一浏览器配置模块，源码环境固定使用 Playwright 安装在项目虚拟环境内的 Chromium，打包环境从 PyInstaller 临时目录解析同一浏览器。
- 运输证查询、营运企业回填和爱企查查询均通过统一路径启动 Chromium，不再依赖本机 Chrome、Edge 或其他 Chromium 浏览器。
- 业务启动前检查内置 Chromium 是否存在，缺失时显示明确的安装命令；README 同步增加源码环境安装步骤。
- 已确认 Playwright 与 DrissionPage 均成功启动同一 `chromium-1208` 可执行文件，并确认 PyInstaller 收集结果包含浏览器目录及 `chrome.exe`。
- 已在将弃用警告视为错误的模式下执行全部离线回归测试，`19/19` 通过且无警告。
- 状态：已完成。

### 步骤 59：按账号角色设置登录默认页

- 管理员登录后继续默认显示数据仪表盘，保留全局统计总览入口。
- 普通用户登录后默认进入“一键业务处理”，减少站点用户开始业务操作前的页面切换。
- 默认页切换复用现有 `show_page()`，确保页面标题、侧边栏选中状态和仪表盘刷新逻辑保持一致。
- 增加管理员与普通用户默认页面、标题和导航选中状态测试。
- 状态：已完成。

### 步骤 60：普通用户开放跨站点统计查看

- 普通用户的数据仪表盘开放“全部站点”和其他站点选择，不再将站点筛选锁定为当前账号。
- 普通用户首次进入仪表盘仍默认选中自己的站点；主动切换后刷新页面会保留当前选择。
- 完成类型、违规原因和最近统计记录统一支持当前站、其他站及全部站点三种范围。
- 管理员继续默认显示全部站点，账号管理权限保持不变。
- 增加普通用户默认站点、跨站完成统计、违规原因和全部站点汇总测试。
- 状态：已完成。

### 步骤 61：新增个人中心并精简身份展示

- 主窗口右上角只显示“用户身份 · 用户名称”，不再显示登录账号或修改密码按钮。
- 侧边栏左下角只保留用户名称，移除身份、账号和退出登录入口。
- 为管理员和普通用户统一新增“个人中心”导航页，集中展示用户身份、用户名称和登录账号。
- 修改密码与退出登录操作迁移到个人中心，继续复用现有密码对话框、任务安全停止和退出确认流程。
- 增加管理员及普通用户身份文案、个人资料字段、导航选中状态和默认页面回归测试。
- 状态：已完成。

### 步骤 62：本人修改密码增加原密码校验

- 个人中心修改密码对话框增加“原密码”输入框，并继续要求输入新密码和确认密码。
- 数据库层使用现有 PBKDF2 密码哈希验证原密码，验证成功后才写入新密码哈希并清除强制改密标记。
- 原密码错误时保留对话框并将焦点返回原密码输入框，不修改现有密码。
- 管理员重置他人密码及首次登录强制改密保持原流程，不要求管理员掌握用户原密码。
- 增加错误原密码、正确修改、新旧密码登录结果及对话框字段测试。
- 状态：已完成。

### 步骤 63：重设计账号管理页面

- 增加账号总数、管理员、普通用户和正常账号四个概览卡片，账号变化后实时刷新。
- 将账号操作与列表整合到统一内容卡片，突出新建账号主操作并集中排列重置、启停操作。
- 优化表格行高、列宽、对齐和无网格样式，为角色、状态及首次改密状态增加差异化颜色。
- 增加当前选中账号信息提示，启用与停用按钮使用不同语义色；当前登录账号不可在管理页重置或停用，引导前往个人中心修改密码。
- 保留账号创建、密码重置、启停及最后管理员保护逻辑。
- 增加概览数量、表格样式、账号选择、操作按钮语义及当前账号保护测试。
- 状态：已完成。

### 步骤 64：精简并美化完整数据页面

- 移除数据仪表盘的“最近统计记录”页签，完成类型和违规类型统一只保留“图表分析 / 完整数据”；关闭文档页签模式，移除贯穿页签栏的长横线，并去掉内容框左上角圆角，让选中标签与下方白色内容框形成连续轮廓。
- 为完整数据页增加当前站点与数据分类说明、实时数据行数标识，筛选切换后同步更新。
- 优化完整数据表格的表头、行高、列宽、留白、交替底色和整行选中效果，隐藏冗余网格与序号列。
- 为账号状态、完成数量、电话分类和占比增加语义化配色，提高数据辨识度。
- 更新仪表盘页签数量、完整数据样式及筛选信息回归测试。
- 状态：已完成。

### 步骤 65：反转完成类型展示顺序

- 图表中的完成类型改为从“有公司名、有电话”依次展示到“空”。
- 完整数据保持“总计数”在首位，其余完成类型按与图表相同的反向顺序展示。
- 全部站点汇总列、单站累计明细和概览指标统一采用新的展示顺序。
- 增加图表分类、汇总列和单站明细顺序回归测试。
- 状态：已完成。

### 步骤 66：新增各站数据分布

- 在数据分类中新增“各站分布”，固定按全部普通站点汇总，进入该分类时暂时锁定站点筛选。
- 使用两个并列环形图展示各站总计数占全部站点总数的比例，以及有电话数占全部站点有电话总数的比例。
- 两个环形图为同一站点复用相同颜色，右侧图例同时展示两组数量和占比，并支持环形分区悬停详情；分区详情仅保留数量和占比，图例四项详情使用 288 像素卡片并缩小紧凑网格列间距，完整展示字段文字并预留字体缩放余量。
- 增加站点数量、总计数、有电话数和整体有电话率概览指标。
- 完整数据同步展示站点、账号、两类数量及对应占比，并使用蓝色、绿色区分两组指标。
- 离开各站分布后恢复进入前选择的站点，避免改变用户原有筛选上下文。
- 增加分类入口、占比计算、完整数据和筛选恢复回归测试。
- 状态：已完成。

### 步骤 67：精简三个分类的完整数据

- 三个数据分类的完整数据均不再展示登录账号和账号状态等管理字段。
- “按完成类型 + 全部站点”改为按完成类型汇总全部普通站点的累计数量，不再按站点拆分成多行。
- 单站完成类型继续展示完成类型、累计数量和单位；违规类型继续展示原因及电话拆分数据。
- 各站分布完整数据移除账号列，仅保留站点名称、总计数、有电话数及两项占比。
- 更新三类表头、全站累计值和各站占比列回归测试。
- 状态：已完成。

### 步骤 68：各站分布设为默认分类

- 将“各站分布”移动到数据分类下拉框第一项，并作为仪表盘默认数据分类。
- 管理员进入应用后的默认仪表盘直接显示各站分布环形图和全站概览。
- 普通用户首次打开仪表盘同样默认显示各站分布；切换其他分类时恢复其本人站点筛选。
- 将测试中的分类切换改为按分类标识定位，避免依赖固定下拉框序号。
- 状态：已完成。

### 步骤 69：全局禁用控件鼠标指针

- 为整个 Qt 应用安装统一事件过滤器，按钮、下拉框、输入框及其他控件禁用时自动显示禁止指针。
- 全局补充禁用态淡化规范：普通、主操作和危险按钮统一转为低对比度灰色，输入控件、标签文字、参数卡片和内容容器同步减弱背景、边框与文字颜色。
- 控件运行中动态启用或禁用时立即同步指针，无需在各页面重复设置。
- 控件重新启用后恢复原指针；原本使用自定义指针的控件同样可以正确还原。
- 主程序启动和测试直接创建主窗口两种入口均安装该规则。
- 增加默认禁用控件、动态启停和自定义指针恢复回归测试。
- 状态：已完成。

### 步骤 70：修复 Windows 禁用指针未实际显示

- 确认 Windows 下禁用的 Qt 控件不会接收鼠标进入事件，仅设置控件 cursor 属性时仍可能显示父容器的普通指针。
- 增加全局鼠标位置轮询，实时识别鼠标下方的实际控件；命中禁用控件时使用应用级禁止指针覆盖。
- 鼠标离开禁用控件后立即撤销覆盖，恢复控件或系统原有指针。
- 使用 Windows 原生平台实际移动鼠标验证：禁用下拉框返回 `ForbiddenCursor(14)`，移到启用下拉框后覆盖恢复为空。
- 扩展自动测试，覆盖应用级指针覆盖和撤销逻辑。
- 状态：已完成。

### 步骤 71：管理员修改用户名称

- 账号管理页新增“修改名称”操作，选中账号后可修改用户名称，登录账号保持不变。
- 新增名称编辑对话框，显示当前登录账号并限制用户名称为 1 至 64 个字符。
- 数据库新增按账号编号更新显示名称的方法，校验账号存在性并返回更新后的账号信息。
- 修改普通用户名称后，账号列表和数据仪表盘站点名称立即刷新；预置站点初始化不会覆盖管理员设置的新名称。
- 修改当前管理员名称后，窗口标题、右上角身份、侧边栏、个人中心及账号管理上下文同步更新。
- 增加名称持久化、输入校验、站点名称保留和界面身份同步测试，并完成 Windows 实际渲染检查。
- 状态：已完成。

### 步骤 72：管理员修改权限与迁移站点数据

- 账号管理页新增“修改权限”操作，管理员可将非当前账号切换为管理员或普通用户；数据库继续保护最后一个可用管理员，避免系统失去管理入口。
- 选中普通用户站点后可导出或导入 JSON 数据包，数据包包含指标定义、统计事件及业务明细，不包含密码哈希、密码盐或其他登录凭据。
- 为统计事件增加可迁移的唯一标识，并自动迁移已有数据库；同一数据包重复导入同一站点时自动跳过已存在事件，避免重复累计。
- 导入前校验文件格式、版本、事件结构和 100 MB 大小上限，写入成功后立即刷新各站分布和完整数据。
- 增加权限切换、最后管理员保护、导入导出、敏感字段排除、跨站数据复制、重复导入和界面操作回归测试。
- 状态：已完成。

### 步骤 73：违规原因仅按竖线分隔

- 将新流程结算与数据库历史汇总的违规原因规则统一为仅按半角竖线 `|` 分隔，不再按半角或全角斜线拆分。
- `恶意U/J形行驶` 等名称中的斜线现在作为类型名称的一部分完整保留，不会错误拆成两个违规类型。
- 历史事件中已被旧规则保存为“恶意U”和“J形行驶”的同值重复分类会自动合并；同时兼容真实旧数据中“恶意U”仍与前一个 `|` 分类保存在同一原始键内的情况，合并后只计数一次。
- 单行内使用 `|` 重复填写的同一原因仍会去重，既有电话与其他数据统计口径保持不变。
- 增加流程写入和数据库历史汇总回归测试，覆盖斜线保留及竖线拆分。
- 状态：已完成。

### 步骤 74：业务处理设置分区与内容折叠

- 将一键业务处理页面拆分为“业务处理 / 运行设置”两个标签页，文件、步骤、进度和结果区域保留在主业务标签，处理参数集中到独立设置标签；页签统一采用白色圆角内容框、左上角直连和蓝色选中态，与下方内容区域融为一体。
- 为内置 Chromium 增加异步健康检查：后台实际启动无头浏览器、访问空白页并关闭测试实例，界面展示检测中、运行正常或检测失败状态以及浏览器版本和路径。
- 浏览器检测说明和可执行文件路径默认收起，可通过“查看详情 / 收起详情”按钮按需展开，重新检测不会打断当前展开状态。
- 处理参数区域重构为“运行模式、验证码策略、查询策略”三张参数卡片，统一标题、说明、间距和重试次数单位，并随模式切换展示对应运行说明。
- 全局统一下拉框、复选框和次数输入框样式：增加悬停、聚焦、禁用反馈，定制下拉箭头、勾选标记和数值调节按钮，并将 SVG 控件资源纳入 PyInstaller 打包配置。
- 移除 `QComboBoxPrivateContainer` 的 Qt 原生外框，避免下拉列表的自定义圆角边框外再次出现一层方形边框。
- Windows 仍会为 `Popup` 窗口绘制系统边缘，因此启用无边框、无原生阴影和透明背景标志并清零弹出容器边距，只保留自定义圆角边框。
- 下拉列表会覆盖在原下拉框位置，展开时额外隐藏下拉框本体及箭头区边框，避免本体顶部轮廓从列表后方露出。
- Windows/Fusion 样式下弹出窗口的几何顶边与列表可见边框之间仍包含内边距，因此增加全局弹出定位：列表在下方时按顶部内边距向上微移，在上方时按底部内边距向下微移，并同步处理屏幕左右边界。
- 为兼容页面显示后才创建或装配的下拉框，弹出容器与所属控件改用 Qt 动态属性关联，并在页面显示事件中递归注册全部下拉框，确保定位规则稳定触发。
- 根据界面实测保留弹出列表圆角，并为整个弹出窗口增加 8 像素圆角裁剪遮罩，使圆角内容层覆盖并裁掉 Windows/Qt 可能绘制的外层直角边缘；列表展开时下拉框本体保持完整圆角边框。
- 下拉框本体保持完整显示，仅让弹出窗口与控件边缘产生等同于列表内边距加 1 像素的几何重叠，以抵消透明边缘和圆角裁剪造成的视觉空隙，使列表可见边框贴合控件。
- 每次打开业务处理页面后，仅在首次进入运行设置标签时自动检查；后续切换标签不再重复检查，可通过“立即检测”手动重试；流水线启动前必须存在一次成功的运行检查。
- 数据预览和流水线日志改为两个独立可折叠面板，可分别收起或展开，并在折叠后自动把可用空间让给另一面板。
- 修复两个面板同时收起时上方业务表格和步骤卡片被剩余空间纵向拉伸的问题，剩余空间改由专用占位区域吸收。
- 增加设置标签、浏览器检查状态和两个折叠面板的自动测试；完整离线回归 `30/30` 通过，并使用真实内置 Chromium 完成启动检查。
- 状态：已完成。

### 步骤 75：管理员重置用户统计数据

- 账号管理页为普通用户站点新增“重置统计”危险操作；未选中账号或选中管理员时保持禁用。
- 执行前明确显示站点名称、登录账号和不可恢复提示，并默认选中取消，防止误操作。
- 数据库按用户编号事务性删除全部统计事件及事件内业务明细，同时保留账号、密码、权限、启停状态和全局指标定义。
- 重置完成后通过既有站点数据变更信号立即刷新数据仪表盘，并提示实际清除的事件数量。
- 增加数据库隔离性、账号保留、管理员目标保护和界面确认流程回归测试；完整离线回归 `31/31` 通过。
- 状态：已完成。

### 步骤 76：账号删除与统计日期范围

- 账号管理页新增“删除账号”危险操作，允许管理员删除非当前账号；执行前默认取消并明确提示账号、统计事件及业务明细均不可恢复。
- 数据库使用单个事务清除目标账号统计、解除其创建者关联并删除账号，同时保护系统中的最后一个管理员。
- 为预置站点增加删除标记，管理员删除后应用再次启动不会自动将该账号重新创建；管理员手动新建同名账号时可正常恢复。
- 新增可复用日期范围控件，默认选择“全部日期”；取消后可选择包含首尾当天的开始和结束日期，日期顺序会自动校正。
- 数据仪表盘的各站分布、完成类型、违规原因和完整数据统一按所选日期范围刷新，管理员与普通用户使用相同筛选口径。
- 账号管理页将统计数据操作独立为一行，导出、导入和重置共用所选日期范围；导出文件记录范围元数据，导入提示重复与范围外事件数量，重置仅清除范围内数据。
- 日期输入框接入全局圆角、悬停、聚焦、下拉日历和禁用淡化样式。
- 日期范围控件按“开始日期、结束日期、全部日期”排列，并以竖向分隔线和浅蓝色独立区域区分“全部日期”开关。
- 增加跨日期累计、违规明细、迁移、范围重置、账号删除持久性及界面联动测试；完整离线回归 `35/35` 通过。
- 状态：已完成。

### 步骤 77：修复日期文字区域无法打开日历

- 确认日期文字由 `QDateEdit` 内部的只读文本框接收鼠标事件，原整框点击逻辑只覆盖外层控件，因此点击文字无响应、点击内边距才会打开日历。
- 将内部文本框的左键按下、释放和双击事件转发到统一的日历按钮逻辑，使日期文字、留白和下拉箭头均可打开日期选择窗口。
- 内部文本框同步使用手型指针；日期只读、键盘防误改、滚轮防误改及下拉箭头行为保持不变。
- 回归测试改为直接点击日期文字区域；完整离线回归 `37/37` 通过，全项目语法编译通过。
- 状态：已完成。

### 步骤 78：主窗口支持 800 × 600 与页面滚动

- 主窗口最小尺寸由 `1280 × 820` 调整为 `800 × 600`，默认启动尺寸继续保持 `1450 × 920`。
- 在顶部栏下方为整个页面栈增加单一滚动区域；侧边栏和顶部栏保持固定，业务页面按 `1050 × 700` 设计画布展示。
- 窗口空间充足时不显示滚动条；缩小到无法容纳页面画布时，自动显示横向和纵向滚动条，避免筛选、图表、表格和操作区被裁掉。
- 统一美化横向与纵向滚动条：使用透明轨道、圆角滑块，并增加悬停和按下颜色反馈，移除两端箭头按钮。
- 使用 `800 × 600` 实际渲染验证：页面视口为 `558 × 526`，横向和纵向滚动范围分别为 `492` 和 `174`，两条滚动条均正常显示且没有遮挡主导航。
- 增加最小窗口尺寸、页面画布、按需滚动策略、滚动条显示状态和样式规则回归断言；完整离线回归 `37/37` 通过，全项目语法编译通过。
- 状态：已完成。

### 步骤 79：修复仪表盘日期范围在窄布局下相互挤压

- 确认日期范围组件虽然能报告完整最小尺寸，但自身未设置最小宽度，父级横向布局仍会将其从完整宽度压缩，导致日期下拉箭头被相邻控件覆盖、“全部日期”文字显示不全。
- 日期范围组件现在锁定布局计算出的最小宽度；两个日期框、分隔标签、“全部日期”面板和复选框均保留完整可读尺寸，不再参与横向压缩。
- 仪表盘筛选栏改为响应式网格：宽度足够时保持站点、数据分类和日期范围同一行；宽度不足时，完整日期范围自动移动到第二行。
- 在 `800 × 600` 窗口中实际渲染验证：筛选栏进入双行布局，日期范围组件保持 613 像素完整宽度，两个日期框均保持 222 像素宽度，“全部日期”面板保持 101 像素宽度。
- 增加窄窗口筛选布局位置、日期组件最小宽度、日期框宽度和“全部日期”文本区域回归断言；完整离线回归 `37/37` 通过，全项目语法编译通过。
- 状态：已完成。

### 步骤 80：仪表盘筛选项保持单行并增加分组区分

- 根据最新交互要求移除日期范围自动换到第二行的行为，站点、数据分类和日期范围在所有窗口宽度下始终保持同一行。
- 页面设计画布宽度由 1050 调整为 1220 像素；默认 `1450 × 920` 窗口无需横向滚动，较窄窗口通过既有页面横向滚动查看完整筛选行。
- 三类筛选分别放入独立浅色分组卡片，使用圆角边框、背景、悬停反馈和 10 像素组间距形成明确区分；分组标题统一使用加粗灰蓝色文字。
- 两个日期框固定为 160 像素宽度，日期范围整体固定为 489 像素宽度，“全部日期”面板保持 101 像素宽度，确保下拉箭头和文字完整显示。
- 在 `800 × 600` 窗口中实际渲染确认三个分组保持同一纵向坐标并按站点、数据分类、日期范围顺序排列；默认窗口横向滚动范围为 0。
- 更新单行分组、排列顺序、分组样式和日期控件完整宽度回归断言；完整离线回归 `37/37` 通过，全项目语法编译通过。
- 状态：已完成。

### 步骤 81：修复无边框窗口放大时客户区短暂空白

- 确认问题在不含仪表盘图表的流程执行页同样出现，排除图表动画是新区域未显示的主因，定位到 Windows 无边框窗口的原生客户区保留策略。
- `WM_NCCALCSIZE` 在客户区尺寸变化时返回 `WVR_REDRAW`，要求 Windows 对完整客户区执行重绘，避免放大过程中继续显示旧尺寸画面并让新区域短暂露出 DWM 背景。
- 保留 `wParam == 0` 时返回 `0` 的既有规则，不改变无边框、圆角、原生边缘拖动、最大化工作区和最小窗口尺寸行为。
- 增加原生客户区重绘返回值回归断言；完整离线回归 `37/37` 通过，`git diff --check` 通过。
- 状态：已完成。

### 步骤 82：按完成类型不再显示“空”分类

- 从完成类型环形图和条形占比列表中移除“空”，其余五种完成类型继续参与分类总数、占比和悬停详情计算。
- 完成类型指标卡、累计明细表以及基于当前明细表生成的 Excel 导出同步过滤“空”，避免不同展示入口口径不一致。
- 数据库指标和流程计数继续保留“空”，兼容已有历史记录及极端情况下的空单元格数据，但不再出现在完成类型界面中。
- 更新完成类型顺序、明细行和图表悬停回归断言；完整离线回归 `37/37` 通过。
- 状态：已完成。

### 步骤 83：完成类型指标卡调整为每行三张

- “按完成类型”的 6 张指标卡由原来的四列布局调整为三列等宽布局，形成上下两行、每行三张的规整排列。
- 指标卡公共布局支持按分类指定列数；各站分布和按违规类型继续保持原有四列布局，分类切换时同步重置列拉伸比例。
- 增加完成类型两行三列位置以及不存在第四列卡片的回归断言；完整离线回归 `37/37` 通过。
- 状态：已完成。

### 步骤 84：启动前在弹窗内检测浏览器并自动继续

- 移除“请先检测浏览器、检测后重新点击开始”的提示流程；点击一键执行后，在确认表格有效的前提下直接打开浏览器检测弹窗并启动后台检测。
- 检测成功后弹窗自动关闭并继续启动三步流水线，无需用户再次点击开始；检测失败时在原弹窗内展示失败原因。
- 检测弹窗提供“取消”和“直接开始”按钮：“取消”终止本次流水线启动，“直接开始”跳过本次健康检查等待，但仍校验内置 Chromium 文件是否存在。
- 用户在运行设置页已经发起检测时，启动弹窗会复用正在执行的检测任务；提前取消或直接开始后，后台检测仍可安全完成并更新设置页状态。
- 增加取消与直接开始结果映射、检测状态联动及按钮文案样式回归测试；完整离线回归扩展为 `38/38` 并通过。
- 状态：已完成。

### 步骤 85：浏览器检测弹窗增加动态反馈

- 检测进行时，弹窗标题以 320 毫秒间隔循环显示一至三个动态点，明确表示检测任务仍在运行。
- 在说明文字与操作按钮之间增加蓝色不确定进度条，并使用独立圆角、轨道和进度色样式，与现有界面风格保持一致。
- 检测成功或失败后立即停止标题动画并隐藏进度条；失败时将视觉焦点切换到错误原因以及“取消”“直接开始”操作。
- 增加动画帧、按钮结果映射和专用进度条样式断言；完整离线回归 `38/38` 稳定通过。
- 状态：已完成。

### 步骤 86：管理员查看“空”异常数据来源

- 完成类型继续保留数据库中的“空”指标，但默认完成类型图表、指标卡和明细表仍不把异常数据混入正常完成分类。
- 仅管理员在“按完成类型”中可见“异常数据”按钮，按钮随当前站点和日期范围实时显示异常总数；普通用户不创建该入口。
- 管理员进入异常视图后，可按用户（站）查看登录账号、异常条数、本站总计数和异常占比，并可继续使用站点及日期范围筛选。
- 异常视图可直接使用现有 Excel 导出功能，导出元数据的数据分类标记为“异常数据”。
- 增加管理员权限隔离、异常总数、来源站点、站点筛选、导出分类、完成类型返回和普通用户不可见回归测试；完整离线回归扩展为 `39/39` 并通过。
- 状态：已完成。

### 步骤 87：前两步 Excel 改为定向回写和原子保存

- 将运输证查询与营运企业回填中的 `pandas.to_excel()` 整表覆盖全部替换为 `openpyxl` 定向回写，仅修改当前数据行的结果列。
- 运输证查询只写入“运输证号_纯数字”和“查询状态”，营运企业回填只写入“车辆所有人/企业”和“回填状态”；其他单元格、下方未处理数据、其他工作表及现有格式不参与重建。
- 保存时先在原文件同目录生成临时 `.xlsx`，完整写入成功后使用原子替换更新原文件；保存或替换失败时清理临时文件并保留原文件。
- 停止运输证查询时只保存当前行已有结果，停止营运企业回填时保存此前已完成行，不再通过 DataFrame 重写整份表格。
- 增加中间空行定向写入回归测试，覆盖下方数据、无表头列、公式、填充样式、列宽、冻结窗格、其他工作表和合并单元格保留；完整离线回归扩展为 `40/40` 并通过。
- 状态：已完成。

### 步骤 88：已有企业信息时跳过运输证查询

- 运输证查询在既有“已有运输证号”和“已协助补缴”条件之间增加“车辆所有人/企业”非空判断。
- 该列只要包含可见内容，包括真实企业名称、“无运输证号”或“已补缴”等状态文字，就不再访问运输证查询网站，并将查询状态记录为“已有企业信息（跳过）”。
- 空单元格以及 `NaN`、`None`、`null`、`NaT` 等空值文本仍继续执行运输证查询。
- 增加工作线程级回归测试，通过伪浏览器确认已有企业信息时网站访问次数为零、原企业信息保持不变；完整离线回归扩展为 `41/41`。
- 状态：已完成。

### 步骤 89：侧边栏视觉重构与配色更新

- 侧边栏由纯深蓝色改为深青绿纵向渐变，选中项使用薄荷绿色强调边和青绿色背景，品牌与头像使用暖橙色点缀。
- 品牌区增加“运”字徽标和英文版本信息，导航区增加“工作导航”分组标题，并将原字符图标替换为四个统一线性 SVG 图标。
- 导航按钮增大点击高度，补充悬停边框、圆角卡片和左侧选中指示，管理员与普通用户继续按既有权限显示导航项。
- 底部新增本地数据连接状态和账号身份卡，展示名称、角色、登录账号及名称首字头像；当前管理员改名后名称、提示和头像同步更新。
- 四个导航 SVG 已加入 PyInstaller 数据文件，自动测试覆盖资源存在、打包声明、导航文本、图标装配、身份卡及改名联动；完整离线回归保持 `41/41`。
- 使用 Windows 原生 Qt 平台完成 `1450 × 920` 实际渲染检查，品牌、导航选中态、图标和底部身份卡均正常显示。
- 状态：已完成。

### 步骤 90：统一登录页、内容区与弹窗主题

- 移除主窗口右上角“角色 · 用户名称”文字，顶部栏只保留当前页面标题和窗口控制按钮；账号身份继续集中展示在侧边栏底部身份卡中。
- 登录界面、主内容区、卡片、表格、筛选控件、日期控件、进度条、滚动条和无边框弹窗统一切换为青绿、薄荷绿与青灰色体系，消息弹窗图标也按信息青绿、警告暖橙、错误红色统一绘制；布局和交互结构保持不变。
- 数据仪表盘与账号管理中的主指标、选中态、图表主系列和统计数据区同步采用青绿色，异常提示与品牌点缀继续使用暖橙色，成功和错误状态保留语义色。
- 当前账号改名后继续同步窗口标题、侧边栏身份卡、个人中心和账号管理上下文，不再维护已移除的顶部身份控件。
- 更新相关界面断言，并通过 Windows 原生 Qt 分别渲染登录页、`1450 × 920` 主窗口和无边框消息弹窗进行视觉检查；完整离线回归保持 `41/41`。
- 状态：已完成。

### 步骤 91：移除首次启动管理员提示

- 登录页不再显示“首次启动已创建管理员”提示、默认账号或默认密码，也不再自动预填管理员账号。
- 首次运行时后台仍会创建或恢复默认管理员，首次登录后的强制改密安全流程保持不变。
- 登录窗口尺寸、账号密码表单和登录按钮保持不变；提示移除后在内容块上下设置对称弹性空间，使标题、表单与登录按钮整体垂直居中。
- 状态：已完成。

### 步骤 92：增加可恢复的业务批次计时

- 将同一用户、同一路径且原始输入数据一致的业务表格识别为一个逻辑批次；每次点击执行建立独立运行记录，手动停止后再次执行会接续未完成批次的累计有效用时。
- 输入签名排除运输证号、查询状态、企业信息、回填状态以及爱企查目标字段，因此程序写回结果后仍能识别为同一批次；原始输入发生变化时会建立新批次。
- 新增批次、运行、步骤尝试和计时事件四类 SQLite 持久化记录，分别保存有效用时、暂停用时、重试次数、当前步骤、停止原因和异常信息。
- 流水线界面显示本次有效用时及本批次累计有效用时；手动暂停、等待登录、验证码和人工录入时间单独累计，不计入有效用时。
- 运输证验证码、结果页、详情页、营运查询验证码、浏览器重启和爱企查安全验证通过结构化信号记录重试，不依赖解析日志文字。
- 运行期间每 5 秒保存一次心跳；客户端正常退出会等待工作线程保存后结算，进程异常消失时会在下次启动检测并将旧运行标记为异常中断，最多丢失一个心跳周期的时间。
- 增加停止后续查累计、暂停时间隔离、重试事件、进程存活检测和异常中断恢复测试；完整离线回归扩展为 `45/45`。
- 状态：已完成。

### 步骤 93：仪表盘增加计时与人工效率对比

- 仪表盘固定展示总用时、有效用时、每条平均有效用时和较纯人工效率提升四项指标，并随站点及日期范围同步刷新。
- 总用时包含有效运行与暂停等待；只有三个步骤全部成功的批次才进入仪表盘，完成后会连同该批次停止、失败、异常中断及重试前已经产生的用时一起汇总，停止后到再次启动前的间隔不计入。
- 人工基准按 260 条耗时 6 小时，即平均约 83.077 秒/条；效率提升使用 `(人工预计用时 - 系统有效用时) / 人工预计用时` 计算，慢于人工时允许显示负值。
- 每条平均用时只使用能通过任务编号匹配到计时运行的完整流程条数作为分母，避免升级前历史统计缺少对应耗时而虚高效率。
- 增加跨运行状态汇总、日期范围过滤、匹配完成条数和仪表盘卡片计算测试；完整离线回归扩展为 `47/47`。
- 使用 `1450 × 920` 离屏渲染检查，新增效率区域与筛选栏、原统计卡片及图表区域没有重叠。
- 状态：已完成。

### 步骤 94：毫秒计时、小时制展示与全站耗时圆环

- 运行片段按最近 1 毫秒四舍五入后保存，流水线实时标签和批次累计标签统一显示 `HH:MM:SS.mmm`。
- 仪表盘总用时、有效用时和每条平均用时的主值统一使用一位小数小时；鼠标悬停卡片可查看总用时、有效用时、暂停等待、每条平均和人工预计用时的毫秒级明细。
- 各站分布由两个业务量圆环扩展为总计数、有电话数、总耗时、有效耗时四个圆环；耗时圆环按站点耗时占全部普通用户站点对应耗时的比例分段。
- 圆环悬停卡片同步展示一位小数小时、`HH:MM:SS.mmm` 精确耗时和站点占比，站点详情卡同时呈现业务量与两类耗时。
- 新增毫秒舍入、小时制卡片、详细提示和耗时圆环测试；完整离线回归扩展为 `48/48`，并通过 `1450 × 920` 四环布局渲染检查。
- 状态：已完成。

### 步骤 95：浏览器异常自愈、日志保留与旧批次合并

- 流水线开始时不再清空日志，改为保留最近 5000 行，并在每次执行前插入带运行次数的分隔记录，便于回看上一轮失败原因。
- 运输证查询将浏览器关闭、页面上下文失效、导航超时和 HTTP 错误识别为可恢复异常；自动重新创建内置 Chromium 并原地重试当前记录，连续 3 次恢复失败后终止步骤 1，不再把技术异常写入查询状态后继续步骤 2。
- 营运企业回填增加同样的 HTTP 404、浏览器关闭与导航超时恢复机制；浏览器启动也最多重试 3 次，失败信号只发送一次，最终错误原因保留在流水线日志中。
- 对已经受旧问题影响的数据增加自愈：查询状态中存在浏览器技术异常时允许重新查询运输证号；已经取得运输证号后，旧的“无运输证号”企业结果不再阻止营运信息重新回填。
- 批次输入签名忽略结果列之间的全空 `Unnamed` 占位列，并同时生成旧版兼容签名；发现同一路径下由这类空列拆分出的多个未完成批次时，会合并其运行、步骤和计时事件后继续累计。
- 使用实际问题表格和本地数据库只读核对，确认旧、新签名分别对应两个被拆分批次；新增关闭浏览器恢复、HTTP 404 恢复、日志保留、空占位列兼容与旧批次合并测试，完整离线回归扩展为 `52/52`。
- 状态：已完成。

### 步骤 96：完成批次结算、结果列归位与人工基准修正

- 仪表盘计时改为按成功批次结算：三个步骤没有全部成功时不展示该批次用时；最终成功后，再将同批次此前停止、失败、异常和重试产生的有效及暂停时间统一计入。
- 日期范围以完整流程统计事件的完成日期筛选，未完成或已放弃的错误表格不再给仪表盘增加总用时、有效用时和运行次数。
- Excel 定向写入器会识别“已协助补缴”后的带格式空列，优先将运输证号、查询状态和回填状态写入这些真实空列；旧版已追加到右侧的结果列会连同现有数据自动迁回，且不会覆盖无表头但包含数据的列。
- 人工效率基准修正为 260 条耗时 6 小时，即平均约 83.077 秒/条；效率卡片、说明文字和悬停明细同步更新。
- 增加未完成批次隔离、完成后累计旧运行、带格式空列迁移和新效率基准回归测试；完整离线回归扩展为 `53/53` 并通过。
- 状态：已完成。

### 步骤 97：延长第二步等待并消除验证码加载竞态

- 营运查询页面、验证码处理和结果返回的等待上限由 30 秒统一延长到 60 秒，慢网络下不再过早进入失败或人工兜底流程。
- 点击查询后不再固定等待 0.5 秒并立即判断；程序会区分无需验证码的直接结果和验证码弹窗，并检查题目文字、图片元素尺寸及图片资源加载状态。
- 验证码图片首次就绪后增加 800 毫秒稳定窗口并再次确认，只有连续两次确认完整加载才允许 OCR 或人工点击流程继续。
- 验证码通过后仍会等待公司名称或无营运信息提示真正出现，不再直接读取尚未返回的公司字段；只有页面已经明确返回但公司字段为空时才允许人工兜底，纯技术超时不会误弹人工输入框。
- 增加验证码图片延迟加载、短暂就绪后回退以及企业结果延迟返回测试；完整离线回归扩展为 `54/54` 并通过。
- 状态：已完成。

### 步骤 98：修复仪表盘站点占比图例文字重叠

- 将站点占比图例由固定 27 像素行高改为根据站名和占比两种字体的实际行高计算布局，站名、数量/用时占比和相邻站点之间均保留独立间距。
- 站名使用加粗字体区分主次信息；图例高度不足时动态减少可见站点，并为“另有 N 个站点”提示预留空间。
- 按问题截图的 `597 × 246` 尺寸完成原生 Qt 渲染检查，五个站点的两行文字均清晰显示且没有交叠。
- 增加图例文字矩形互不重叠的回归测试；完整离线回归扩展为 `58/58` 并通过，`git diff --check` 通过。
- 状态：已完成。

### 步骤 99：均衡账号列表左右列宽

- 修正账号列表仅让“用户名称”和“账号”拉伸、导致右侧五列集中显示的问题。
- “用户名称”“账号”“创建时间”“最后登录”现在共同分配剩余表格宽度；内容较短的“角色”“状态”“首次改密”继续按内容宽度显示。
- 增加账号表格列宽策略回归断言，避免后续样式调整再次把时间列压缩到右侧。
- 状态：已完成。

### 步骤 100：精简并美化用时指标悬停信息

- 将四张用时指标卡原有的系统黄底长文本提示替换为项目统一的深色圆角悬停卡片。
- 各指标只显示与自身相关的精确用时、完成数据或提升比例，不再重复展示整段计时口径说明。
- 从效率区标题、悬停卡片和用时明细表中移除运行次数与具体人工基准字样，效率计算逻辑保持不变。
- 更新用时指标悬停交互、精简内容和敏感文案移除的回归测试。
- 状态：已完成。

### 步骤 101：优化计时悬停卡片字段间距

- 计时指标悬停卡片改为单列明细，字段名统一补充中文冒号，并在字段与数值之间增加留白，避免时间数字紧贴冒号。
- “较纯人工效率提升”悬停卡片恢复显示人工预计用时，但继续隐藏具体人工基准字样。
- 增加冒号格式、字段右侧间距、单列行数和人工预计用时回归断言。
- 状态：已完成。

### 步骤 102：平均指标改为总用时处理效率

- 将“每条平均用时”改为“平均处理效率”，使用完整流程总用时而非有效用时作为计算分母。
- 指标卡和用时明细统一显示“条/小时”；悬停卡片反向换算并显示一位小数的“秒/条”。
- 更新界面说明、Excel 导出数据和回归断言，确保处理效率与平均耗时使用同一总用时口径。
- 状态：已完成。

### 步骤 103：整数处理量与总用时效率提升

- 平均处理效率改为整数“条/小时”，避免业务条数出现小数；悬停卡片同时保留一位小数“秒/条”和精确到毫秒的平均用时。
- 较纯人工效率提升改为使用总用时计算，不再只使用有效用时；悬停卡片展示总用时、人工预计用时和完成数据。
- 同步更新用时明细、Excel 导出、README 与回归断言。
- 状态：已完成。

### 步骤 104：优化仪表盘中心文字与指标图标

- 环形图中心标题和查询总数改用独立文本区域布局，固定保留垂直间距，避免文字与数字贴合。
- 顶部五张指标卡不再使用“Σ”“今”“时”等字体字符，统一改为查询、日历、电话、无电话和时钟矢量图案。
- 增加图标类型、非文字控件和中心文字间距的回归断言。
- 状态：已完成。

### 步骤 105：延长业务页面等待并取消浏览器恢复次数限制

- 运输证、营运查询和爱企查结果页的浏览器页面等待上限统一调整为 2 分钟。
- 任一步骤的浏览器被手动关闭、页面异常、导航超时或 HTTP 错误时，不再因达到固定次数而终止；程序会持续重启内置 Chromium 并重试当前记录，直到恢复成功或用户主动停止。
- 浏览器本身启动失败时同样持续重试，并在重试间隔内保持暂停和停止操作可响应，避免形成不可中断的紧密循环。
- 扩展三步浏览器恢复测试，连续四次浏览器异常后仍可恢复并完成当前记录。
- 全项目语法检查和完整离线回归 `59/59` 通过。
- 状态：已完成。

### 步骤 106：业务计时毫秒级平滑刷新

- 将业务处理页的计时显示刷新间隔由 1 秒缩短为 50 毫秒，使毫秒部分随实际计时连续变化，不再整秒跳动。
- 将界面显示刷新与持久化心跳拆成两个独立定时器；数据库心跳仍保持每 5 秒一次，不因界面高频刷新而增加写入频率。
- 增加精确定时器类型、刷新间隔、心跳间隔及毫秒文本回归测试。
- 全项目语法检查和完整离线回归 `60/60` 通过。
- 状态：已完成。

### 步骤 107：补齐爱企查登录等待阶段的浏览器自动恢复

- 爱企查等待用户登录和点击“继续执行”期间每 200 毫秒检查一次页面与浏览器存活状态，不再只等待界面按钮事件。
- 浏览器被手动关闭或连接中断时立即回到会话创建循环，不限次数自动重新打开内置 Chromium，并再次进入登录提示。
- 用户确认登录前后均执行存活检查，避免点击“继续执行”与浏览器关闭同时发生时把失效页面带入正式查询。
- 增加登录等待期间连续关闭四次浏览器、第五次仍可自动打开并继续执行的回归测试。
- 全项目语法检查和完整离线回归 `61/61` 通过。
- 状态：已完成。

### 步骤 108：进一步细化业务计时刷新

- 将业务处理页的计时显示刷新间隔由 50 毫秒缩短为 16 毫秒，约按 60 帧/秒更新三位毫秒显示。
- 继续使用 `time.perf_counter` 作为高精度计时源；16 毫秒不是 10 的整数倍，毫秒末位会呈现更多连续变化，不再长期只在少数固定数字间跳动。
- 数据库持久化心跳仍保持每 5 秒一次，高频界面刷新不会增加数据库写入次数。
- 更新计时刷新间隔回归断言。
- 全项目语法检查和完整离线回归 `61/61` 通过。
- 状态：已完成。

### 步骤 109：停止时主动中断爱企查页面加载

- 修复爱企查首页或搜索页仍处于 DrissionPage 同步加载时，停止操作只能设置标志、工作线程要等到页面超时才退出的问题。
- 点击停止后会先唤醒登录和暂停等待，再从当前会话摘除浏览器，并在独立守护线程中调用 `stop_loading()` 和关闭浏览器，避免阻塞界面线程。
- 浏览器创建、页面加载和异常恢复路径增加停止竞态检查；停止过程中不再启动新会话或记录一次误导性的浏览器重试。
- 增加先关闭浏览器、再点击停止时同步首页加载可立即被打断且线程正常结束的回归测试。
- 全项目语法检查和完整离线回归 `62/62` 通过。
- 状态：已完成。

### 步骤 110：增加保护数据的强制退出

- 任务在正常停止等待期后仍未结束时，提示框由单一“确定”改为默认的“继续等待”和危险操作“保存并强制退出”。
- 强制退出会先请求所有线程停止，保存第三步已完成的爱企查结果并结算当前步骤、运行计时，只有全部保护步骤成功后才允许终止卡住的线程。
- 第三步 Excel 保存改为同目录临时文件加 `os.replace()` 原子替换，与前两步的逐行原子保存保持一致；保存失败会清理临时文件、保留原表并取消强制退出。
- 强制终止后仍有线程拒绝退出时增加短延迟进程级兜底，确保用户选择的强制退出能够完成。
- 增加原子保存成功、替换失败保护原表且不终止线程、强制退出提示按钮和默认选择回归测试。
- 全项目语法检查和完整离线回归 `65/65` 通过。
- 状态：已完成。

### 步骤 111：增加开发者专用打包发布器

- 新增只在开发者电脑运行的 PyQt5 发布器，集中提供版本字段同步、环境预检、
  客户端与服务端测试、便携包/完整包/增量包构建、本地发布、SSH 远程发布、
  发布确认、实时日志、停止流程和发布快照归档。
- 发布器配置保存在当前 Windows 用户目录，不保存更新说明、强更选择、密码、
  令牌或私钥内容；远程发布要求 Git 工作区干净，已存在发布快照的版本不可
  覆盖。
- 修复组合构建脚本把增量来源参数传给便携包、却未传给安装包的问题，并改用
  PowerShell 调用状态判断，确保子脚本没有设置外部进程退出码时仍会继续执行
  安装包构建。
- 远程发布前读取现有通道清单，相同或更高版本已经发布时拒绝覆盖或降级。
- 发布清单记录源代码 Git 提交及本地准备包的工作区状态；远程发布脚本会再次
  拒绝未提交工作区。
- 新增发布器核心、版本同步、GUI 和组合构建参数路由测试；客户端完整回归
  `122/122`、服务端回归 `27/27`、Python 编译检查、Ruff 和 6 个 PowerShell
  发布脚本语法检查全部通过。
- 状态：已完成。

### 步骤 112：修复取消更新后的界面锁定并保留窗口控制

- 定位到更新弹窗通过 `QDialog.open()` 进入窗口模态后，再在下载开始时动态切换
  非模态，Qt 仍会把已经关闭的弹窗保留为活动模态窗口，导致主窗口输入持续被拦截。
- 更新提示改为非模态窗口，由主窗口仅禁用侧边栏、业务页面和公告入口来模拟提示
  阻塞；最小化、最大化和关闭按钮始终保持可用。
- 下载开始、取消、失败或完成时统一解除业务内容锁定；更新弹窗右上角关闭改为调用
  `QDialog` 默认关闭流程，确保发出 `finished` 信号并完成状态清理。
- 保留下载过程中关闭主窗口时的“停止更新并退出”确认，以及强制更新提示不可直接
  关闭的原有规则。
- 增加非强制更新提示、下载、关闭确认、取消下载和关闭弹窗的完整状态回归测试；
  客户端完整回归 `143/143` 通过，`git diff --check` 通过。
- 状态：已完成。

### 步骤 113：增加非强制后台更新入口

- 非强制更新弹窗原“取消”按钮改为“稍后更新”，继续只关闭本次提示，不改变
  当前版本的后续提醒设置。
- 新增“后台更新”按钮；下载任务成功启动后自动收起更新弹窗，进度继续显示在
  “系统设置”，下载和校验完成后仍按原流程询问是否重启安装。
- “立即更新”继续保留弹窗内进度和停止下载入口；强制更新不显示“后台更新”
  “稍后更新”或“不再提示”。
- 扩展普通更新取消、后台下载、弹窗收起和强制更新按钮可见性回归测试；客户端
  完整回归 `143/143` 通过。
- 状态：已完成。

### 步骤 114：打包发布器增加暂停分发

- 发布器操作栏新增“暂停分发”，只校验 SSH 主机、远程更新目录、通道和私钥，
  不要求重新构建安装包，也不受待发布版本、更新说明或 Git 工作区状态影响。
- 新增 `pause-update.ps1`：读取活动清单并做前后 SHA-256 竞态校验，把原清单
  完整归档到更新目录同级的非公开目录，再原子替换兼容旧客户端的暂停标记；
  安装包不会删除，已经取得清单或正在下载的客户端不会被强制中断。
- 更新清单 API 对带版本客户端返回与当前客户端版本相同的暂停响应，对无版本
  请求返回 HTTP 204；新版客户端也显式把暂停标记和 204 视为“暂无更新”。
- 发布脚本识别暂停前真实版本，继续拒绝同版本覆盖或降级；成功发布更高版本后
  自动恢复通道分发。
- 增加发布器按钮与参数校验、PowerShell 语法和模拟 SSH 原子切换、服务端暂停
  响应及客户端兼容回归测试；客户端完整回归 `150/150`、服务端完整回归
  `36/36` 通过，Ruff、Python 编译检查和 `git diff --check` 通过。
- 状态：已完成。

### 步骤 115：统一应用对外名称

- 将 Windows 客户端、登录页、主窗口、侧边栏品牌区、PyInstaller 可执行文件、
  安装器、快捷方式和 Windows 版本资源中的应用名称统一改为“逃费车辆智能查询平台”。
- 安装器保留原 AppId、安装目录、更新包名和内部 `IntDemo` 标识，并在升级时精确
  清理旧名称的可执行文件及开始菜单、桌面快捷方式，避免破坏现有安装和本地数据。
- README 标题及产品介绍同步使用新名称，`IntDemo` 仅作为内部项目代号保留。
- 增加应用名称与安装器、版本资源一致性测试；客户端完整回归 `151/151`、Python
  编译检查、版本字段检查、`git diff --check` 和 Inno Setup 6.7.3 实际编译冒烟均通过。
- 状态：已完成。

### 步骤 116：准备 v0.2.8 名称更新发布

- 将客户端、服务端、安装器、Docker 镜像标签和构建脚本的权威版本字段统一从
  `0.2.7` 提升到 `0.2.8`，README 当前版本同步更新。
- 本次更新使用 `test` 通道、非强制更新，生成完整安装包及精确适用于 `0.2.7`
  的增量安装包；发布说明聚焦应用更名、旧名称清理和现有数据兼容。
- 发布前客户端完整回归 `151/151`、服务端完整回归 `36/36`、客户端 Python
  编译检查、服务端 Ruff、版本字段检查和 `git diff --check` 全部通过。
- 状态：已完成。

### 步骤 117：增加统信 UOS Desktop 20 ARM64 首轮兼容

- 新增 UOS ARM64 专用 conda 环境、依赖清单、PyInstaller onedir 配置、环境准备、
  诊断、构建以及用户级安装/卸载脚本；目标基线为 UOS Desktop 20 1070、
  `aarch64`、glibc 2.28、Wayland，并使用隔离的 Python 3.10 环境，不依赖系统
  Python 3.7。
- Linux 端优先自动发现统信浏览器或 Chromium，也支持通过
  `INTDEMO_CHROMIUM_PATH` 指定浏览器；Wayland 会话默认借助 XWayland 运行 Qt
  与 Chromium，并保留原生 Wayland 覆盖入口。
- 在线凭据在 Windows 继续使用 DPAPI，在 Linux 改用 Secret Service 保存随机主密钥，
  再以 AES-GCM 加密本地数据；UOS 配置文件支持 XDG 标准目录。
- UOS 端暂不启用只适用于 Windows 安装包的自动更新，改为手动下载、覆盖安装；
  中文字体栈补充 Noto Sans CJK、文泉驿和 DejaVu 回退。
- 修复 GitHub Actions 从 PowerShell 7 启动 Windows PowerShell 5.1 测试时继承
  不兼容 `PSModulePath` 的问题，避免发布脚本版本保护测试无法加载
  `Get-FileHash`；生产发布脚本不受影响。
- 新增真机搭建、诊断、构建、安装和验收文档；本地客户端完整回归 `160/160`、
  Python 编译检查、PyInstaller spec 编译检查及 Bash 脚本语法检查通过。
- 状态：首轮代码兼容已完成，等待 UOS ARM64 真机构建和业务流程验证。

### 步骤 118：将真机验证通过的 ARM64 Chromium 装入 UOS 包

- 在 UOS Desktop 20 Professional 1070、aarch64、glibc 2.28、Wayland 真机上，
  项目 Python 3.10 环境的全部原生依赖成功导入；Playwright Chromium 145、
  Secret Service 加解密及无头浏览器实际启动检查均通过。
- 环境准备脚本默认把固定 Playwright 版本对应的 Chromium 下载到项目专用
  `.playwright-uos-arm64` 缓存；源码运行自动识别该缓存，并继续保留系统浏览器
  和 `INTDEMO_CHROMIUM_PATH` 作为回退与故障排查入口。
- 构建脚本只允许使用项目缓存内的 Chromium，预检 ELF AArch64 架构和缺失
  动态库，只复制正式 Chromium 运行目录而不携带无用的浏览器缓存，并在压缩前
  对复制后的浏览器再做一次实际启动检查。
- UOS 软件包启动器优先使用随包携带的 `browser/chrome`，最终用户无需安装
  系统浏览器；文档、包内说明、缓存忽略规则和静态回归同步更新。
- UOS 专项回归 `12/12`、客户端完整回归 `163/163`、Python 编译、Bash 语法及
  `git diff --check` 通过。
- 状态：内置浏览器构建代码已完成，等待 UOS 真机生成软件包并验收业务流程。

### 步骤 119：修复 UOS 真机构建前的跨平台回归失败

- 首次 UOS 真机构建日志确认环境更新、全部依赖导入、Chromium AArch64 ELF、
  动态库、Chromium 145 实际启动及 Secret Service 加解密均通过，构建只在
  离线回归测试阶段中止，尚未进入 PyInstaller 打包。
- 发布器读取当前分支由 Git 2.22 才提供的 `git branch --show-current` 改为旧版
  Git 也支持的 `git rev-parse --abbrev-ref HEAD`，并继续显式拒绝 detached HEAD，
  兼容 UOS 20 自带 Git。
- UOS 浏览器路径测试创建临时可执行文件后显式赋予执行权限，修复 Linux 会严格
  检查执行位而 Windows 不检查所造成的三项测试环境差异。
- 违规模块悬浮卡测试不再要求不同明细数量的卡片在所有字体环境下高度完全相等，
  改为验证内容对应的最低高度和固定宽度，保留实际布局与可见性检查。
- 针对失败点的专项回归 `7/7`、客户端完整回归 `165/165` 通过。
- 状态：真机构建前阻断已修复，等待 UOS ARM64 重新构建并生成软件包。

### 步骤 120：修复 UOS 成品 Qt 的 GLIBCXX 运行库冲突

- UOS 真机成功生成首个 `0.2.8` ARM64 压缩包，但从压缩包全新解压启动时，
  PyInstaller 收集到的系统旧版 `libstdc++.so.6` 缺少 conda-forge Qt 5.15
  所需的 `GLIBCXX_3.4.26`，应用在导入 `PyQt5.QtCore` 时立即退出。
- UOS conda 环境显式加入 ARM64 `libgcc-ng` 与 `libstdcxx-ng`；PyInstaller 完成后，
  构建脚本强制用该环境中已经在 glibc 2.28 真机成功加载 Qt 的 `libstdc++.so.6`
  和 `libgcc_s.so.1` 替换误收集的系统版本。
- 出包前新增 `GLIBCXX_3.4.26` 符号检查、最高 GLIBCXX ABI 记录、Qt Core `ldd`
  缺失库及实际解析路径检查，确保 Qt 使用包内 C++ 运行库。
- 客户端增加不会进入登录和数据库流程的 `--self-check`，构建脚本会通过最终包
  启动器以 Qt offscreen 模式执行它；导入或 Qt 插件加载失败将直接中止构建。
- UOS 专项回归 `13/13`、客户端完整回归 `166/166`、Python 编译、Bash 语法、
  本地 Qt 自检及 `git diff --check` 通过。
- 状态：C++ ABI 修复和成品级防回归已完成，等待 UOS ARM64 真机重新出包启动。

### 步骤 121：修复 Qt 运行库路径误判和旧产物残留

- 真机确认 conda 与新 PyInstaller 目录中的 `libstdc++.so.6` 均已升级到
  `GLIBCXX_3.4.35`，Qt 的 `ldd` 结果也实际指向包内库；ABI 修复本身生效。
- Qt RPATH 输出的路径包含等价的 `/_internal/./libstdc++.so.6`，原构建校验却与
  不含 `/./` 的字符串直接比较，造成误判并在成品组装前中止。
- 新校验从 `ldd` 输出提取实际路径，并通过 `readlink -f` 与目标包内库的真实路径
  比较，既接受等价路径，又继续拒绝意外解析到系统或其他目录的 C++ 运行库。
- 同版本旧压缩包此前直到组装阶段才删除，失败后仍保留了 09:49 的旧包；现改为
  在预检和 PyInstaller 开始前清理经过版本格式与目录边界校验的旧包、校验文件及
  旧组装目录，失败构建不再留下名称相同的陈旧产物。
- 状态：等待 UOS ARM64 真机快速重建并验证新压缩包启动。

### 步骤 122：为 UOS 成品包补全在线测试服务配置

- UOS Desktop 20 Professional 1070 ARM64 真机确认新成品可以正常进入登录页，
  游客模式下的实际业务处理全部正常；Qt、内置 Chromium 和 GNU C++ 运行库链路
  已通过，剩余阻点是成品包没有在线服务器配置。
- 从现有 Windows 测试包确认在线测试地址为 `https://43.138.177.65`，并复用同一
  公开 Caddy 根证书；证书标准 DER SHA-256 指纹为
  `1ba425e2184fe9bab3e90620773ec9bc89aaf292f84c00cf54b5a0901c4bf69f`。
  使用该证书访问服务端 `live` 与 `ready` 健康接口均返回 HTTP 200；服务端当前
  自报版本为 `0.2.7`。
- UOS 压缩包现会携带 `client-online.json` 和公开根证书。根启动器依次采用显式
  环境变量、用户配置和随包配置；用户安装脚本只在配置不存在时复制默认值，升级
  不覆盖已有环境。软件包不包含账号、密码、令牌或私钥。
- 构建脚本在成品自检前加载一次随包在线配置，静态回归同时固定服务器地址、证书
  相对路径和 DER 指纹，避免出包时漏复制或误替换；测试不依赖 PEM 文件换行符，
  因此兼容 Windows CRLF 与 Linux LF 检出方式。
- UOS 专项回归 `15/15`、客户端完整回归 `168/168`、Python 编译、Bash 语法、
  在线配置加载、服务端健康检查及 `git diff --check` 通过。
- 状态：默认在线配置已完成，等待 UOS 真机验证账号登录、记住密码、重启解密和
  数据同步；客户端 `0.2.8` 与当前服务端 `0.2.7` 的最终兼容性以该轮结果为准。

### 步骤 123：增加 UOS ARM64 原生 DEB 分发包

- 在已经通过真机验证的便携包基础上新增独立 `build-deb.sh`，无需重新运行
  PyInstaller 即可快速重封装；完整 `build.sh` 默认同时生成 `tar.gz` 和同版本
  `.deb`，并继续提供 `--skip-deb` 故障排查入口。
- DEB 包名和 AppID 统一为 `com.e23aqiu.intdemo`，架构固定为 `arm64`；应用按
  UOS 规范放入 `/opt/apps/com.e23aqiu.intdemo/` 下的 `files`、`entries` 与
  `info` 结构，桌面入口直接启动包内根启动器。
- DEB 复用当前仓库中的启动器、在线配置与公开根证书，因此即使基于此前生成的
  ARM64 便携成品重封装，也会带上最新连接配置；已有用户配置仍然优先。
- 控制信息声明 UOS 桌面运行依赖和 ARM64 架构，生成 `md5sums`、包级 SHA-256，
  使用 `--root-owner-group` 或 `fakeroot` 固定所有权，并在打包后复查包名、版本、
  架构和入口清单。包中没有 `postinst`，不会修改用户目录或删除业务数据。
- 首次从 `~/.local/opt/intdemo-client` 切换到 DEB 时，文档要求先运行原用户卸载
  脚本清理旧程序和桌面入口；数据库、浏览器资料、Secret Service 密钥及在线配置
  均继续保留在 XDG 用户目录。后续可由软件包安装器完成升级和卸载。
- 在 WSL 原生 Linux 文件系统中实际生成最小测试 DEB，`dpkg-deb` 成功读取
  `Package=com.e23aqiu.intdemo`、`Version=9.9.9`、`Architecture=arm64`，应用入口、
  在线配置、根证书、UOS `info` 和桌面文件的归档路径及权限检查通过。
- UOS 专项回归 `16/16`、客户端完整回归 `169/169`、Python 编译、Bash 语法和
  `git diff --check` 通过。
- 状态：DEB 构建与静态/最小归档验证完成，等待 UOS ARM64 真机用现有完整成品
  生成约 400 MB 的实际包，并验收双击安装、应用菜单、覆盖升级和卸载保数。

### 步骤 124：修复 UOS V20 的 DEB 菜单入口缓存冲突

- UOS Desktop 20 Professional 1070 ARM64 真机确认 DEB 安装成功，直接执行
  `/opt/apps/com.e23aqiu.intdemo/files/intdemo-client` 可以正常显示登录界面，但
  复用历史用户安装桌面 ID 的原菜单项点击后没有反应。
- 真机新建唯一桌面 ID 的诊断入口后，DDE 菜单可以正常执行同一 DEB 根启动器；
  诊断日志确认 `HOME`、`PATH`、Wayland、X11 显示变量、内置浏览器、在线配置及
  Qt XCB 设置均正常，因此问题限定为旧菜单 ID 缓存，而非权限或运行库失败。
- DEB 桌面文件改为 `com.e23aqiu.intdemo.uos.desktop`，显示名增加“（UOS）”，并
  在旧版 DDE 下关闭启动通知；历史用户安装继续使用原桌面 ID，两种分发入口不再
  相互遮盖。构建后新增桌面入口归档校验，防止重封装时退回旧文件名。
- 继续遵循 UOS 应用目录规范：全部应用文件仍位于 `/opt/apps/${appid}`，不写入
  `/usr/share`、不增加 `postinst`、不修改任何用户目录。文档补充同版本测试包需
  使用 `apt --reinstall` 刷新 `dpkg` 文件清单。
- UOS 专项回归 `16/16`、客户端完整回归 `169/169`、Python 编译、Bash 语法和
  `git diff --check` 通过；WSL 原生 Linux 文件系统中的最小 DEB 实际归档确认只
  包含新的 UOS 桌面 ID，包名、版本与 ARM64 架构校验继续生效。
- 状态：该独立桌面 ID 方案后续真机验证未通过，纠正过程见步骤 125。

### 步骤 125：恢复 UOS AppID 同名入口并从干净环境重验

- 真机强制重装独立桌面 ID 的 DEB 后，DDE 同时显示遗留旧入口与新 UOS 入口，
  两者点击均没有启动；此前成功的诊断入口位于用户应用目录，并未经过 UOS
  `/opt/apps` 应用注册链路，因此不能证明独立系统桌面 ID 可用。
- 结合 UOS 打包规范中“应用入口一般以 AppID 命名”及该入口会由系统链接、后续经
  沙箱启动的约束，撤销 `com.e23aqiu.intdemo.uos.desktop`。DEB 恢复唯一入口
  `com.e23aqiu.intdemo.desktop`，与包名、`info.appid` 及 Qt DesktopFileName 完全
  一致；显示名仍保留“（UOS）”，并继续关闭旧 DDE 启动通知。
- 构建脚本继续校验桌面入口实际进入 DEB；静态回归新增桌面文件名与 `info.appid`
  相等的断言，并拒绝重新引入独立 UOS 文件名。
- 根启动器新增最小 `launcher.log`，仅记录被执行的 desktop 文件、包根目录和桌面
  会话，并捕获应用入口启动前的标准错误；它不记录账号、令牌或业务内容。下一轮
  若仍点击无反应，可直接区分“UOS 未执行入口”和“入口执行后应用退出”。
- 真机已主动删除全部历史安装、用户与系统菜单入口以及 `dist` 产物，下一轮将从
  干净源代码执行完整 ARM64 构建和首次安装，排除旧 DDE 链接及缓存干扰。
- UOS 专项回归 `16/16`、客户端完整回归 `169/169`、Python 编译、Bash 语法、
  启动日志行为及 `git diff --check` 通过；WSL 原生文件系统中的最小 ARM64 DEB
  实际归档确认只包含 AppID 同名桌面入口，独立 UOS 文件名不存在。
- 状态：AppID 同名入口已恢复并通过回归，等待干净真机完整重建与首次安装。

### 步骤 126：修复 UOS 菜单注入 Qt Wayland 平台导致的静默退出

- 干净环境完整重建并首次安装后，AppID 同名菜单入口仍点击无界面；新增的
  `launcher.log` 证明 UOS 已正确执行 `/usr/share/applications/com.e23aqiu.intdemo.desktop`，
  包路径、用户和显示变量均正常。
- 日志捕获到 UOS 菜单环境注入了 `QT_QPA_PLATFORM=wayland`，而随包 Qt 只包含
  `eglfs`、`minimal`、`offscreen`、`vnc`、`webgl` 与 `xcb` 插件，因此 Qt 在创建
  QApplication 前立即退出。终端启动能够成功，是因为终端环境没有该注入值，旧
  启动器的 `${QT_QPA_PLATFORM:-xcb}` 才会选择 `xcb`。
- 根启动器和 Python 平台兼容层现把 UOS Wayland 会话中继承的空值、`wayland` 或
  `wayland-egl` 统一回退到 `xcb`，同时保留构建自检所需的 `offscreen` 等明确值。
  新增 `INTDEMO_QT_QPA_PLATFORM` 作为有意验证原生 Wayland 的唯一显式覆盖入口。
- 启动日志追加最终生效的 Qt 平台与 Chromium Ozone 平台，后续无需再从启动前环境
  猜测实际选择。
- UOS 专项回归增至 `19/19`、客户端完整回归增至 `172/172`，Python 编译、Bash
  语法和 `git diff --check` 通过；真实 Bash 启动器测试确认系统注入 `wayland`
  会得到 `xcb`，`offscreen` 保持不变，项目专用原生 Wayland 覆盖仍然生效。
- 状态：根因与修复已落地；由于 ARM64 便携成品已经重新生成，只需快速重封装并
  强制重装 DEB，无需再次运行 PyInstaller。

### 步骤 127：隔离 UOS Qt 5.11 插件与随包 Qt 5.15

- UOS 真机继续出现菜单启动退出和直接运行返回 `139`。对照自检确认
  `offscreen+compose` 正常，而 `xcb+compose`、`xcb+fcitx` 均发生段错误，因此
  排除了 Wayland 注入和 Fcitx 输入法本身是必要触发条件。
- `QT_DEBUG_PLUGINS=1` 进一步确认随包 Qt 5.15.15 实际加载了
  `/usr/lib/aarch64-linux-gnu/qt5/plugins/imageformats/kimg_*.so`；这些插件元数据版本
  `330499` 对应 UOS Qt 5.11.3，崩溃位置同时落在 `QApplication` 初始化和窗口图标
  加载路径。根因是为发现系统 Fcitx 插件而把整个系统 Qt 插件根目录加入了
  `QT_PLUGIN_PATH`。
- ARM64 构建现只把系统 Fcitx 平台输入上下文复制到 PyInstaller 私有
  `PyQt5/Qt5/plugins/platforminputcontexts` 目录，并用包内运行库执行 `ldd`，拒绝
  缺失依赖或解析到包外 Qt。运行时会过滤继承到的系统 `QT_PLUGIN_PATH` 与
  `QT_QPA_PLATFORM_PLUGIN_PATH`，保留应用自有插件路径。
- 成品自检会显式创建输入法上下文，使用 `QT_DEBUG_PLUGINS` 确认私有 Fcitx 插件
  已加载且没有发现任何系统 Qt 插件；构建会话存在 `DISPLAY` 时，再分别执行
  `xcb+compose` 与 `xcb+fcitx` 两次真实图形初始化，防止仅靠 `offscreen` 漏检。
- UOS 专项回归 `22/22`、客户端完整回归 `179/179`、Python 编译、本机 Qt
  `offscreen` 自检、Bash 语法和 `git diff --check` 均通过。
- 状态：代码和静态/本机回归完成，等待 UOS ARM64 真机重新完整构建，以私有插件
  加载日志及两次 XCB 自检结果作为出包条件；当前有问题的 UOS 0.2.9 不应发布。

### 步骤 128：以统信 ARM64 作为 Windows/UOS 双端发布主控

- 双端发布器支持单选或全选构建 Windows x64 EXE 与 UOS ARM64 DEB；UOS 真机在
  本地生成并校验 DEB，Windows 默认先导出不可变任务，再交给 GitHub Actions
  完成测试、安装包、可选增量包/便携包和候选快照构建。
- GitHub CLI 缺失、认证失败、连接中断、等待超时或结果下载失败时使用专用退出码，
  保留不含部署凭据的请求 ZIP，并提示转 Windows x64 真机。个人 Windows 只需获取
  精确 Git 提交和构建依赖，不连接 IntDemo 程序服务器，结果可通过批准的介质带回。
- 新增 Windows 请求/结果格式，绑定完整源提交、版本、服务地址、通道、公开 CA、
  增量来源、便携包选项及所有产物 SHA-256；UOS 导入后生成收据，正式发布要求同一
  提交和配置的 Windows/UOS 两个平台收据。
- Windows 增量任务携带实际已发布快照，拒绝缺失快照或重建旧提交代替；新候选
  快照只在远程双端清单成功安装并通过完整哈希复核后转为正式发布快照。
- UOS 发布命令检查服务端平台清单能力、远程版本防覆盖、同名文件哈希、上传后
  哈希与并发清单变化，并最后原子安装清单；UOS 收据还会解开实际 DEB，复核其中
  的提交、在线配置和 CA。暂停分发也提供跨平台 Python 路径。
- 新增 UOS GUI 启动器、Windows 真机辅助脚本、GitHub Windows workflow、统信主控
  操作手册和任务安全回归。UOS 打包/SSH 发布不依赖 Docker，服务端容器部署保持
  独立。

### 步骤 129：隔离 UOS 打包前测试与发布器用户配置

- 发布器 UI 测试不再读取开发者真实的 `settings.json`，统一使用独立默认配置，
  避免用户已选择 `stable` 通道时测试仍固定断言 `test` 而中止 UOS 构建。
- Windows 暂停分发计划测试显式模拟 Windows 主控，不再在 UOS ARM64 上错误要求
  PowerShell `pause-update.ps1`；UOS 原生 Python 暂停流程继续由专项计划覆盖。
- 发布器专项回归 `26/26`、客户端完整回归 `195/195` 和静态检查通过。

### 步骤 130：Windows/UOS 对称构建结果与双端发布主控

- Windows 与 UOS 的“构建所选安装包”都会保留可直接安装的 EXE/DEB，并同时生成
  可跨机器导入的标准结果 ZIP；Windows 本机构建不再把候选快照误写为正式发布
  快照。
- 新增 UOS 结果 ZIP 格式，记录版本、完整 Git 提交、服务地址、通道、CA 指纹、
  构建机信息以及 DEB 的文件名、大小和 SHA-256；导入会拒绝路径越界、文件篡改、
  配置不一致和提交不一致。
- Windows 和 UOS 发布器都开放“导入 Windows 构建包”和“导入统信构建包”。
  Windows 远程 UOS 构建会同时下载裸 DEB 与标准 UOS 结果 ZIP，再自动导入收据。
- 两种主控统一使用 Python 双端发布流程，正式发布都必须同时具备 Windows/UOS
  校验收据、可访问更新 API，并填写发布服务器 SSH 主机；上传、防覆盖、并发清单
  替换和正式快照归档规则完全一致。
- 发布器专项与安全回归 `49/49`、客户端完整回归 `209/209`、服务端回归 `37/37`、
  Python 编译、PowerShell 语法和差异检查均通过。
- 本轮准备发布的客户端、服务端、安装器、Docker 镜像和构建脚本版本统一升级为
  `0.2.11`。

### 步骤 131：支持独立导出安装包与发布器结果包

- 发布器新增“输出类型”：可单独导出 Windows EXE/UOS DEB、单独生成可跨机器导入的
  Windows/UOS 标准结果 ZIP，或一次生成两者；一键双端发布自动固定为“两者”。
- UOS 仅结果包模式复用同版本已生成的 DEB 并重新校验后封装；Windows 仅结果包模式
  在结果链路中构建 EXE 并把它放入结果 ZIP，安装包模式则使用轻量安装包脚本。
- “导出 Windows 构建任务”改名为“导出 Windows 构建请求包”，界面和文档明确它是
  交给 Windows 真机执行的输入，不是最终 EXE 或可导入结果包。
- 定向发布器回归 `51/51`、客户端完整回归 `210/210`、服务端回归 `37/37`、Python
  编译和 PowerShell 语法检查通过。

### 步骤 132：合并重复的 Windows 请求包入口

- 移除操作栏中独立的“导出 Windows 构建请求包”按钮，避免与“Windows 真机任务包”
  模式下的主导出按钮重复。
- 请求包底层能力、GitHub 不可用时自动保留请求包以及 Windows 真机执行流程保持
  不变；选择真机模式后，主按钮会明确显示“导出 Windows 构建请求包”。
- 发布器定向回归 `51/51` 通过。

### 步骤 133：为双端正式发布增加大文件断点续传

- 正式发布由一次性 `scp` 上传改为系统 OpenSSH `sftp reput`：EXE、可选增量包、
  DEB 和更新清单均先写入远程 `.part` 文件，连接中断时保留已上传字节。
- SSH 统一启用批处理模式、20 秒连接超时、15 秒服务端保活、TCP 保活和最大 20 次
  保活容错；单文件上传最多自动尝试 6 次，并按短暂退避继续上次远端字节位置。
- 版本、完整源提交及产物名称/大小/SHA-256 共同生成稳定临时目录。同一组产物重新
  点击发布会复用原断点；已达到完整大小的断点先校验 SHA-256，正确时直接复用，
  错误时只清空该精确文件重新上传。
- 发布器日志约每 10 秒显示已上传容量和百分比，发布确认、停止提示及双端操作文档
  均说明自动重试和重新发布续传行为；正式发布环境检查改为要求 `ssh + sftp`，
  远程 UOS 构建和暂停分发的 `scp` 要求保持不变。
- 新增稳定断点标识、SSH 保活参数、SFTP `reput`、部分文件续传、自动重试、完整
  断点复用/纠错、多次失败保留以及正式文件防覆盖回归。发布器专项 `61/61`、客户端
  完整回归 `221/221`、服务端回归 `37/37` 与 Python 编译检查均通过。

### 步骤 134：v1.0.2 公告中心与用时口径优化

- 顶栏公告喇叭改为打开全部公告列表，提供未读状态、发布时间、正文预览和详情入口；
  公告轮播使用独立的圆角悬停卡，展示标题、摘要、发布人和当前轮播序号。
- 步骤 2 启动 Chromium 及加载营运查询页面期间自动暂停业务计时；加载完成后恢复，
  与人工暂停状态兼容，浏览器或页面加载等待不再计入有效用时。
- 数据中心暂时移除独立的有效用时卡，新增总用时/有效用时口径切换；首张用时卡、
  平均处理效率、较纯人工效率提升、明细、导出和各站耗时图统一跟随当前口径，悬停
  明细同步显示所选口径。
- 客户端、服务端、安装器、Docker 镜像和构建脚本版本统一升级为 `1.0.2`。
- 客户端完整回归 `235/235`、服务端回归 `37/37`、UOS 专项 `27/27`、Python 编译、
  服务端 Ruff、依赖检查、PowerShell/Bash 语法、Docker Compose 配置和差异检查通过。

### 步骤 135：修复双平台客户端 CI 环境差异

- Windows 客户端测试显式启用 Python UTF-8 模式和 UTF-8 标准输出，避免 GitHub
  Windows runner 的 CP1252 控制台无法输出中文构建日志。
- 发布器界面测试使用固定的 Gitee 测试仓库地址，不再依赖执行环境是否配置 `gitee`
  Git remote；GitHub Actions 只有 `origin` 时也能覆盖完整的安全推送确认流程。
- 原失败用例定向回归 `34/34`、客户端完整回归 `235/235` 和差异检查通过。

### 步骤 136：用时效率图增加动态站点计量条

- 各站用时效率图保留左侧站点耗时占比圆环，将右侧站点明细改为与完成类型一致的
  彩色计量条；条长按当前总用时或有效用时占比计算，颜色与圆环分段保持一致。
- 计量条复用现有流光动画和整行悬停详情，总用时/有效用时口径切换后会同步更新
  标题、耗时、占比、条长及精确用时提示；窄窗口会自动缩小圆环区域并重新分配
  站名和耗时列宽，避免右侧内容被裁切。
- Windows 原生渲染检查、定向界面回归、客户端完整回归 `235/235`、UOS 专项
  `27/27` 和差异检查通过。

### 步骤 137：对齐用时效率圆盘与动态计量条

- 用时效率图改用与“完整流程结果分布”一致的布局基准：圆盘固定从左侧同一位置
  开始并使用相同的最大尺寸，右侧计量条紧接圆盘展开，消除两者之间的大块空白。
- 计时模式移除重复的圆盘小标题和列表表头，面板副标题精简为“总用时占比”或
  “有效用时占比”，不再显示“圆环与动态计量条同步展示”。
- 保留流光动画、总用时/有效用时切换、圆盘悬停和整行悬停详情；新增布局坐标回归
  断言，Windows 原生渲染复核通过，客户端完整回归 `235/235` 通过。

### 步骤 138：发布版本升级为 v1.0.3

- 将用时效率圆盘与动态计量条对齐优化作为 v1.0.3 版本内容，并新建
  `codex/v1.0.3` 分支，保留既有 v1.0.2 分支及历史记录。
- 客户端、服务端、Windows 安装器、四个 Windows 构建脚本和 Docker 镜像标签
  统一升级为 `1.0.3`；Windows 文件版本同步为 `1.0.3.0`，UOS 构建继续动态读取
  客户端主版本源，无需平台专用硬编码。
- 版本一致性与残留版本检查、客户端完整回归 `235/235`、服务端回归 `37/37`、
  服务端 Ruff、Python 编译、PowerShell 语法和 Docker Compose 配置检查均通过。

### 步骤 139：验证码即时采集与 v1.0.4

- 数字和文字点选验证码改为网页确认通过后立即上报，不再等待后续业务结果；成功
  样本上传前先写入按服务器账号隔离的本地 SQLite 队列，失败时保留，重传成功后
  自动删除，避免断网导致样本丢失。
- 侧栏在线状态卡在“采集样本并统计”策略下将原待同步区域等分为业务待同步与
  验证码待同步按钮，后者展示队列数量并支持手动重传；关闭或仅统计策略保持原有
  单按钮样式。Windows Qt 原生渲染检查确认双按钮等宽且文字完整。
- 新建 `codex/v1.0.4` 分支，将客户端、服务端、安装器、构建脚本和 Docker 镜像
  标签统一升级为 `1.0.4`，Windows 文件版本同步为 `1.0.4.0`。
- 版本一致性与残留版本检查、客户端完整回归 `236/236`、服务端回归 `37/37`、
  服务端 Ruff、Python 编译、PowerShell 语法、Docker Compose 配置和验证码真实
  Qt 异步失败重传演练均通过；客户端完整回归包含 UOS ARM64 兼容性测试。

### 步骤 140：修正步骤 2 浏览器加载期间的总用时显示

- 步骤 2 等待浏览器加载时继续暂停有效用时，但业务页的本批次总用时改为实时累加
  有效用时与暂停等待，加载期间总用时会继续走动。
- 计时快照显式提供本次和本批次总用时，完成后的持久化统计口径保持“有效用时＋暂停
  等待”不变，并新增浏览器加载期间有效用时静止、总用时递增的回归覆盖。
- 客户端完整回归 `236/236`、UOS ARM64 兼容性回归 `27/27` 和 Python 编译检查
  通过。

### 步骤 141：修复营运企业信息已显示仍持续等待

- 步骤 2 的企业名称结果兼容读取输入框当前值和普通文本节点，解决网页已将企业名称
  写入 `input.value`、但原逻辑只读节点文本而一直等待到超时的问题；后续回填公司名
  复用同一读取口径。
- 保留验证码可见性和结果字段可见性校验，避免隐藏模板字段或上一条记录的旧值提前
  结束当前查询；新增输入框结果、普通文本回退和验证码未消失时不误判的回归覆盖。
- 客户端完整回归 `238/238`、UOS ARM64 兼容性回归 `27/27`、Python 编译和差异检查
  通过。

### 步骤 142：适配营运企业结果表格新版结构

- 交通运输部营运车辆查询页将企业名称从旧版 `#ownerName2` 字段迁移到
  `tbody#licenseBody` 结果表格；步骤 2 改为优先扫描该表格的可见数据行并读取第二个
  单元格，从而在验证码通过且页面已展示企业信息时立即结束等待。
- 新逻辑跳过隐藏的历史/模板行以及只有一个合并单元格的加载或空结果占位行，只有
  读取到非空企业名称才判定结果就绪；未找到新版表格结果时继续回退旧版
  `#ownerName2`，兼容网页新旧结构。
- 新增新版表格、隐藏旧行、单格占位行和旧版输入框回退测试；客户端完整回归
  `239/239`、UOS ARM64 兼容性回归 `27/27`、Python 编译和差异检查通过。

### 步骤 143：修复文字点选样本采集、分类数据集与爱企查加载计时

- 文字点选验证码原先只在 `.back-img` 元素监听点击，而网页实际点击层位于图片上方，
  导致验证码通过后只有成功统计、没有图片和坐标样本。现改为在文档捕获阶段监听图片
  范围内的点击，并在事件点数不足时从网页生成的 `.point-area` 编号标记反算归一化
  坐标；仍不完整时写入明确日志，避免静默丢弃原因无法判断。
- 机器学习管理页保持单一数据集导出入口，服务端在同一个 ZIP 内使用
  `images/numeric/` 和 `images/click/` 分类保存两类图片，并在清单中记录目录和数量。
  导出的原 ZIP 可直接导回，同时继续兼容旧版无分类目录的数据集。
- 步骤 3 新增爱企查浏览器加载边界：首页启动、异常重建和每条企业搜索结果页的同步
  导航及等待均暂停有效计时，加载完成后自动恢复；这些暂停区间继续累加到本次和批次
  总用时，并与人工暂停、登录等待及验证码等待保持兼容。
- 新增覆盖层点选采集与点位标记兜底、单包分类数据集原格式导出/导回、爱企查加载信号和
  有效/总计时口径测试；客户端完整回归 `242/242`、服务端回归 `37/37`、UOS ARM64
  兼容性回归 `27/27`、Python 编译和差异检查通过。

### 步骤 144：确认当前模型业务生效、兼容备注列并升级 v1.0.5

- 核验“应用所选模型”全链路：服务端切换当前版本后，客户端立即刷新策略、下载模型、
  校验 SHA-256 并安装到与业务线程共享的模型管理器；步骤 1 数字验证码优先调用当前
  数字模型，步骤 2 文字点选验证码使用当前点选模型分类检测区域，失败才回退内置
  `ddddocr`。管理页和业务日志新增“已下载校验/已使用当前模型”的明确反馈。
- 业务表补缴标记列兼容新版“备注”和旧版“已协助补缴”；任一列有内容时继续沿用原
  跳过规则，两列同时存在时也不会漏判。定向写入器优先以新版“备注”为结果列锚点，
  工作流入口只要求两种列名至少存在一种。
- 新建 `codex/v1.0.5` 分支，将客户端、服务端、安装器、构建脚本和 Docker 镜像统一
  升级为 `1.0.5`，Windows 文件版本同步为 `1.0.5.0`。
- 客户端完整回归 `245/245`、服务端回归 `37/37`、UOS ARM64 专项 `27/27`、发布流程
  回归 `66/66`、Python 编译、版本一致性和差异检查通过。

### 步骤 145：修复点选验证码提交后的成功判定竞态

- 保留原有点选完成后快速检查和“点击错误”日志节奏，不再通过仍存在的点选标记延长
  等待；网页刷新后的新验证码即使被用户迅速点完，也不会再被误认为上一张验证码成功。
- 第一次检查仍显示验证码时，暂存本次提交前采集的原图、题目和点击坐标，但暂不上传
  失败统计。若下一状态直接变为“验证码通过”，使用这份待确认数据上传成功样本；若
  程序进入下一轮“等待用户点完验证码”，才确认上一份失败并将其丢弃。
- 自动点选恢复原有两秒确认和刷新重试逻辑，不与人工样本的待确认状态混用。新增
  “点击错误后直接通过”和“点击错误后进入下一轮等待”两种日志序列回归覆盖；客户端
  完整回归 `247/247`、UOS ARM64 专项 `27/27`、关键静态检查、Python 编译和差异
  检查通过。

### 步骤 146：完善自动识别准确率、样本管理和分类数据集

- 线上准确率聚合只统计 `assisted=false` 的自动识别尝试，人工输入数字验证码及人工
  点选均不进入准确率分子或分母；数字和文字点选分别显示当前自动识别准确率。模型表
  固定展示两类内置 `ddddocr`，并显示训练模型及其他运行时模型各自的自动识别结果。
- 机器学习页新增采集样本表，支持按数字/文字点选筛选、分页、Ctrl/Shift 多选并永久
  删除指定样本；服务端新增管理员样本列表和批量删除接口，删除后样本不再参与导出和
  训练，但保留既有自动识别准确率历史。
- 移除策略状态中的“策略修订 xx”文字。数据集 ZIP 改为在顶层明确创建“数字验证码”
  和“文字点选验证码”两个中文目录，空分类也保留目录；清单引用新路径，原 ZIP 可直接
  导回，同时继续兼容旧版英文目录数据集。
- 客户端完整回归 `248/248`、服务端完整回归 `38/38`、UOS ARM64 专项 `27/27`、
  服务端 Ruff、Python 编译和差异检查通过。
