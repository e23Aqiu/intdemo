# Python 3.10 / glibc 2.28 ARM64. Use only native aarch64 wheels from the
# administrator-approved offline wheelhouse; never build this package on x64.
torch==2.7.0+cpu
onnx==1.18.0
onnxruntime==1.19.2
numpy==2.0.2
Pillow==11.3.0
cryptography==45.0.7
pyinstaller==6.16.0
